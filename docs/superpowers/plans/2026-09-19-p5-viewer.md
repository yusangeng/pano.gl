# P5 — viewer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development（无 subagent 则 executing-plans）；收尾走 superloop 的 task-finish，禁用 finishing-a-development-branch。

**Goal:** 组装出 `FramelessImageViewer` / `FramelessVideoViewer`，**公开 API 冻结**，全部用户故事级集成测试绿。

**Architecture:** `Viewer` **持有**各层（组合，不是 mixin），`dispose()` 显式按「先停生产者再拆消费者」逐个调用。rAF 循环只在有变化时渲染，静止的图片渲染一次就停。

**Tech Stack:** TypeScript · Playwright（每个 User Story 一个文件）

---

## 与旧 API 的差异（写代码前必须知道）

| 旧 | 新 | 理由 |
|---|---|---|
| `el` | `container` | 旧名字是缩写；**这是破坏性改名**，趁大版本做 |
| `projection: 'equiprectangular'` | `'equirectangular'` | 旧值是错字（spec §3.6） |
| `frameSize` | 删 | WebGPU 无 2 的幂要求（P4 前置说明） |
| `new FramelessImageViewer({...})` | `await FramelessImageViewer.create({...})` | 取 GPU device 是异步的，构造函数不能 `await`；spec §2.3 写的是 `constructor`，这里**故意偏离**并写进 JSDoc |
| `new Viewer({...})` 可能「成功」但永不渲染 | 构造失败**必抛** | spec §6.3 |
| `static probe(): Promise<Capabilities>`（spec §2.3） | `Promise<SelectedCapabilities>`，即 `Capabilities \| { backend: 'none' }` | 「一个后端都没有」用 `Capabilities` 表达不出来，而抛异常会让「先探测再决定」这种最正常的用法变成 try/catch；返回 `none` 让调用者自己选 |
| `viewer.PTZ` | 不变 | —— |
| `viewer.src` / `viewer.cameraOptions` | 不变 | —— |

---

## 前置依赖（不在本计划里，但本计划写不出来）

1. **P3 的 `Backend.onDeviceLost(fn: (lost: DeviceLost) => void): () => void` 必须存在。** 设备丢失要变成 `viewer.on('device-lost', …)` 事件，中间必须有这条通道：`Viewer` 是唯一知道「谁该被通知、该按什么顺序拆」的人，但它拿不到 `device.lost` —— 只有持有 device 的后端能。P3 现在**已经**在 `Backend` 接口上声明了它并返回退订函数，本计划的 Task 3 直接用；**如果那条声明被删掉，Task 3 无解，先补 P3**。
2. **P4 的 `MediaSource` 必须带 `frame` / `naturalSize` / `on` / `markFramePresented`。** `frame` 是一个 `MediaFrame`（`state` + `kind` + `element` + `version`），字段与 P3 的 `RenderableSource` 逐一对应，所以 `Viewer` 可以把它**原样**交给 `backend.setSource`，不做转换。
3. **`src/core` 没有汇总出口。** P2 建的是 `src/core/types.ts` / `validate.ts` / `matrix.ts` / `reference.ts` / `constants.ts`，**不要在本计划里造一个 `src/core/index.ts` barrel** —— 直接按模块 import。barrel 会让「谁依赖谁」变得看不出来，而这一层的依赖方向是本设计最在意的东西。
4. **P5 不重算任何投影公式。** CPU 侧的投影只有一处权威：P2 的 `core/reference.ts`（`project` / `ndcToSurface`）。相机矩阵由 P3 的后端按 P2 的 `core/matrix.ts` 构建，`Viewer` 只传 `CameraState` 和 `Projection`。
5. **两个素材 fixture 必须先存在：`public/fixtures/panorama.png`（等距柱状，上下两半颜色可区分）与 `public/fixtures/clip.mp4`（首帧同样上下可分）。** 本计划与 P4 的所有集成测试都从 `/fixtures/...` 取它们，由 Playwright 配置里的 `vite` dev server（P1）从 `public/` 提供。**没有任何一份计划负责生成这两个文件** —— 见 P4「前置说明」第 3 条；动手前先确认它们在仓库里，否则两个计划的集成测试全部跑不起来。

---

## File Structure

| 文件 | 职责 |
|---|---|
| `src/viewer/types.ts` | `CameraOptions` —— 相机的构造/查询参数，公开 API 的一部分 |
| `src/viewer/options.ts` | 两个公开类的构造参数校验（含已被删掉的选项） |
| `src/viewer/camera-controller.ts` | 相机状态的唯一持有者：钳制、回绕、dirty，以及由 surface 尺寸派生的 `aspect` |
| `src/viewer/render-loop.ts` | rAF 循环 + 脏检查（没有变化就一次都不画，**没有帧率上限**） |
| `src/viewer/viewer.ts` | 组合各层、dispose 顺序、设备丢失 |
| `src/viewer/image-viewer.ts` | `FramelessImageViewer` |
| `src/viewer/video-viewer.ts` | `FramelessVideoViewer` |
| `src/viewer/backend-factory.ts` | WebGPU → WebGL2 的降级选择 |
| `src/index.ts` | 公开导出（**冻结**） |
| `demo/test-entry-hooks/viewer.ts` | 测试出口：`createImageViewer` / `createVideoViewer` / 像素读取等 |
| `test/integration/support/backend.ts` | 后端环境掩码：让测试能关掉 WebGPU |
| `test/integration/*.test.ts` | 每个 User Story 一个文件（用户 CLAUDE.md 规定） |

---

## 测试入口需要导出什么

集成测试跑在真实浏览器里，通过 `window.__panoTest` 拿被测对象。出口由 `demo/test-entry.ts` 用 `import.meta.glob('./test-entry-hooks/*.ts')` 拼出来（P1 建、P3 起用），本计划加一个 `demo/test-entry-hooks/viewer.ts`。

**不要改 P3 的 `test-entry.ts` 去加字段。** 它把 `PanoTestApi` 设计成**全局接口**，就是为了让后续阶段用 TS 的声明合并各自拓宽：钩子文件里写 `declare global`。P4 和 P6 各有自己的钩子文件，三个阶段共用一个接口就是三个人改同一行。

**本计划需要的就下面这些**（P4 已导出的 `ImageSource` / `VideoSource` / `InputController` / `WheelDeltaMode` 不在其列）：

```ts
// demo/test-entry-hooks/viewer.ts

/*
 * The viewer hooks the integration tests drive.
 *
 * A hook file per phase, merged by `test-entry.ts`'s `import.meta.glob`, so this
 * one touches nothing P3 or P4 owns.
 */

import { FramelessImageViewer } from '../../src/viewer/image-viewer'
import { FramelessVideoViewer } from '../../src/viewer/video-viewer'
import { WebGPUBackend } from '../../src/renderer/webgpu/backend'
import { probe } from '../../src/viewer/backend-factory'
import type { SelectedCapabilities } from '../../src/renderer/capabilities'
import type { Projection } from '../../src/core/types'
import type { Viewer } from '../../src/viewer/viewer'
import type { ImageProjection, VideoViewerOptions } from '../../src/viewer/options'

/** A canvas readback. Plain numbers, so it survives structured cloning. */
export interface Pixels {
  readonly width: number
  readonly height: number
  readonly data: readonly number[]
}

/** What a test may override. Everything else comes from the library's own options. */
export interface ViewerTestOptions {
  readonly src: string
  /**
   * The texture projection. Distinct from `cameraKind`: this one describes how the
   * source's pixels are laid out, the other which camera model renders them.
   */
  readonly projection?: ImageProjection
  /** Which camera model to start with. Defaults to the library's own default. */
  readonly cameraKind?: Projection['kind']
  readonly PTZ?: boolean
  readonly autoplay?: boolean
  readonly loop?: boolean
  readonly muted?: boolean
  /** Container size in CSS pixels. Defaults to 400x200. */
  readonly size?: readonly [number, number]
  /** Stubs `window.devicePixelRatio` for this page before creating the viewer. */
  readonly dpr?: number
}

export interface ViewerTestHooks {
  createImageViewer (options: ViewerTestOptions): Promise<FramelessImageViewer>
  createVideoViewer (options: ViewerTestOptions): Promise<FramelessVideoViewer>
  probe (): Promise<SelectedCapabilities>
  /** Resolves true when the event fires before the timeout, false otherwise. */
  waitFor (viewer: Viewer, type: string, timeoutMs: number): Promise<boolean>
  /** Resolves after `count` animation frames -- "let the loop run a few times". */
  waitForFrames (count: number): Promise<void>
  readCanvas (viewer: Viewer): Pixels
  /** Fraction of pixels that are not (near) black. */
  countNonBlack (pixels: Pixels): number
  /** Largest per-channel difference between two readbacks. */
  maxDiff (a: Pixels, b: Pixels): number
  /** The canvas backing store size, in device pixels. */
  canvasSize (viewer: Viewer): { readonly width: number, readonly height: number }
  /** The container the viewer was created in. */
  container (viewer: Viewer): HTMLElement
  /** The `<video>` element, for lifecycle assertions. Also watched for listeners. */
  videoElement (viewer: Viewer): HTMLVideoElement
  /** A pointer drag on the viewer's canvas, in CSS pixels. */
  drag (viewer: Viewer, dx: number, dy: number): void
  wheel (viewer: Viewer, deltaY: number): void
  /** A valid `Projection` per kind, so a test does not restate the variants. */
  projection (kind: Projection['kind']): Projection
  /** Frames the backend actually rendered since the page loaded. */
  readonly drawCount: number
  /** DOM listeners still bound to `target`. */
  listenerCount (target: EventTarget): number
  /** DOM listeners still bound anywhere under the viewers this page created. */
  liveListenerCount (): number
  /** Destroys the device behind `viewer`, which is how a real device loss is produced. */
  destroyDevice (viewer: Viewer): void
}

// Merges into the interface P3 opened. A *global* interface, which is why this
// is `declare global` and not `declare module '../test-entry'`: the latter would
// declare a module-scoped `PanoTestApi` in this file's view of that module,
// leaving the global one -- the one `Window.__panoTest` is typed by -- untouched.
declare global {
  interface PanoTestApi extends ViewerTestHooks {}
}

// ---------------------------------------------------------------- containers

const containers = new WeakMap<Viewer, HTMLElement>()
const owners = new WeakMap<Viewer, WebGPUBackend>()
const createdBackends: WebGPUBackend[] = []

/**
 * A sized, positioned container.
 *
 * Sized, because the viewer reads `clientWidth`/`clientHeight` for its backing
 * store: a container with no layout would make every viewer 1x1 and every pixel
 * assertion meaningless. Positioned, so `getBoundingClientRect` does not depend
 * on what else the page has in flow.
 */
function makeContainer (size: readonly [number, number] | undefined): HTMLElement {
  const [width, height] = size ?? [400, 200]
  const container = document.createElement('div')
  Object.assign(container.style, {
    position: 'fixed', top: '0px', left: '0px',
    width: `${width}px`, height: `${height}px`
  })
  document.body.style.margin = '0'
  document.body.appendChild(container)
  return container
}

/**
 * Overrides `devicePixelRatio` for this page.
 *
 * `dpr` is page state, not a library option -- the library reads the real value,
 * and this makes the real value 2. It cannot be assigned (it is a getter on
 * `window`), so it is redefined. Each test gets a fresh page, so nothing has to
 * be restored.
 */
function stubDpr (dpr: number | undefined): void {
  if (dpr === undefined) return
  Object.defineProperty(window, 'devicePixelRatio', { get: () => dpr, configurable: true })
}

// ------------------------------------------------------- draw instrumentation

let drawCount = 0

// There is no public counter, and "the canvas stopped being redrawn after
// dispose" is otherwise unobservable from the page: a stopped loop and a loop
// that keeps drawing the same frame look identical.
const originalRender = WebGPUBackend.prototype.render
WebGPUBackend.prototype.render = function (this: WebGPUBackend) {
  drawCount++
  return originalRender.call(this)
}

// The backend is built inside `create()`, so catching the factory is the only way
// to reach its device. Needed because a lost device cannot be faked from outside:
// the viewer's report has to come from a real `device.lost` (spec 11 measured
// that `device.destroy()` really does resolve it).
const originalBackendCreate = WebGPUBackend.create
WebGPUBackend.create = async function (canvas: HTMLCanvasElement) {
  const backend = await originalBackendCreate.call(this, canvas)
  if (backend) createdBackends.push(backend)
  return backend
}

// ------------------------------------------------------ listener accounting

const tally = new Map<EventTarget, number>()
const watched = new Set<EventTarget>()

// No API lists an element's listeners, and the leak this guards against -- a
// handler bound to a canvas that outlives its viewer -- is exactly what a tally
// catches. Tallied for every target and not only watched ones, so a listener
// bound during construction (before anything could be watched) still counts.
const originalAdd = EventTarget.prototype.addEventListener
const originalRemove = EventTarget.prototype.removeEventListener
EventTarget.prototype.addEventListener = function (
  this: EventTarget,
  type: string,
  fn: EventListenerOrEventListenerObject | null,
  options?: AddEventListenerOptions | boolean
): void {
  tally.set(this, (tally.get(this) ?? 0) + 1)
  originalAdd.call(this, type, fn, options)
}
EventTarget.prototype.removeEventListener = function (
  this: EventTarget,
  type: string,
  fn: EventListenerOrEventListenerObject | null,
  options?: EventListenerOptions | boolean
): void {
  tally.set(this, Math.max(0, (tally.get(this) ?? 0) - 1))
  originalRemove.call(this, type, fn, options)
}

/**
 * Starts watching a subtree. Called after `create()`, because the canvas does not
 * exist before it -- and the canvas is precisely the element whose listeners leak.
 */
function watch (container: HTMLElement): void {
  watched.add(container)
  for (const el of container.querySelectorAll('*')) watched.add(el)
}

// ---------------------------------------------------------------- readbacks

const scratch = document.createElement('canvas')

function readCanvas (viewer: Viewer): Pixels {
  const canvas = container(viewer).querySelector('canvas')
  if (!canvas) throw new Error('the viewer has no canvas')
  // Draw into a 2D canvas instead of reading the swapchain texture: the texture
  // belongs to the backend, and a canvas' image data is the one readback that does
  // not depend on backend internals. Downscaled to 64x32 on purpose -- these tests
  // compare frames, they do not grade them.
  scratch.width = 64
  scratch.height = 32
  const ctx = scratch.getContext('2d')
  if (!ctx) throw new Error('no 2d context for the readback canvas')
  ctx.drawImage(canvas, 0, 0, 64, 32)
  return { width: 64, height: 32, data: [...ctx.getImageData(0, 0, 64, 32).data] }
}

function maxDiff (a: Pixels, b: Pixels): number {
  if (a.data.length !== b.data.length) throw new Error('readbacks differ in size')
  let max = 0
  for (let i = 0; i < a.data.length; i++) max = Math.max(max, Math.abs(a.data[i]! - b.data[i]!))
  return max
}

function countNonBlack (pixels: Pixels): number {
  let lit = 0
  for (let i = 0; i < pixels.data.length; i += 4) {
    if (pixels.data[i]! > 8 || pixels.data[i + 1]! > 8 || pixels.data[i + 2]! > 8) lit++
  }
  return lit / (pixels.data.length / 4)
}

// ---------------------------------------------------------------- gestures

function drag (viewer: Viewer, dx: number, dy: number): void {
  const el = container(viewer)
  const rect = el.getBoundingClientRect()
  const x = rect.left + rect.width / 2
  const y = rect.top + rect.height / 2
  const send = (type: string, cx: number, cy: number): void => {
    el.dispatchEvent(new PointerEvent(type, { pointerId: 1, clientX: cx, clientY: cy, bubbles: true }))
  }
  // Stepped rather than one jump: the recogniser reports a delta per move, and a
  // single move would not exercise accumulation.
  send('pointerdown', x, y)
  for (let step = 1; step <= 4; step++) {
    send('pointermove', x + (dx * step) / 4, y + (dy * step) / 4)
  }
  send('pointerup', x + dx, y + dy)
}

function wheel (viewer: Viewer, deltaY: number): void {
  container(viewer).querySelector('canvas')?.dispatchEvent(
    new WheelEvent('wheel', { deltaY, deltaMode: WheelEvent.DOM_DELTA_PIXEL, bubbles: true, cancelable: true })
  )
}

// ------------------------------------------------------------------ helpers

function container (viewer: Viewer): HTMLElement {
  const el = containers.get(viewer)
  if (!el) throw new Error('this viewer was not created through the test hooks')
  return el
}

function videoElement (viewer: Viewer): HTMLVideoElement {
  const el = (viewer as FramelessVideoViewer).element
  watched.add(el)
  return el
}

/**
 * A valid `Projection` per kind.
 *
 * The variants take different fields, so a test that builds one inline has to get
 * the right ones -- `{ kind: 'linear', zoom: 1, extent: [1, 1] }` is a valid
 * cylindrical projection and an invalid linear one. Extents are the legacy quads
 * (cylindrical 1x1, planet/pannini 4x4 -- P2's `LEGACY_EXTENT`); `fov` is in
 * radians, because cuon took degrees and `mat4.perspective` takes radians.
 */
const projections = {
  linear: { kind: 'linear', fov: (70 * Math.PI) / 180, aspect: 1 },
  cylindrical: { kind: 'cylindrical', zoom: 1, extent: [1, 1] },
  planet: { kind: 'planet', zoom: 1, extent: [4, 4] },
  pannini: { kind: 'pannini', zoom: 1, extent: [4, 4] }
} as const

async function createOne<T extends Viewer> (
  factory: (options: VideoViewerOptions) => Promise<T>,
  options: ViewerTestOptions
): Promise<T> {
  stubDpr(options.dpr)
  const el = makeContainer(options.size)
  const before = createdBackends.length

  const viewer = await factory({
    container: el,
    src: options.src,
    projection: options.projection,
    camera: options.cameraKind ? { projection: projections[options.cameraKind] } : undefined,
    PTZ: options.PTZ,
    autoplay: options.autoplay,
    loop: options.loop,
    muted: options.muted
  })

  // After create(), not before: the canvas exists only once the viewer has it, and
  // the canvas is the element whose listeners are being counted.
  watch(el)
  containers.set(viewer, el)
  if (createdBackends.length > before) owners.set(viewer, createdBackends[createdBackends.length - 1]!)

  return viewer
}

const hooks: ViewerTestHooks = {
  createImageViewer: (options) => createOne(FramelessImageViewer.create, options),
  createVideoViewer: (options) => createOne(FramelessVideoViewer.create, options),
  probe,
  waitFor (viewer, type, timeoutMs) {
    return new Promise(resolve => {
      const timer = setTimeout(() => { off(); resolve(false) }, timeoutMs)
      const off = viewer.on(type as never, () => { clearTimeout(timer); off(); resolve(true) })
    })
  },
  waitForFrames (count) {
    return new Promise(resolve => {
      let left = count
      const tick = (): void => { if (--left <= 0) resolve(); else requestAnimationFrame(tick) }
      requestAnimationFrame(tick)
    })
  },
  readCanvas,
  countNonBlack,
  maxDiff,
  canvasSize (viewer) {
    const canvas = container(viewer).querySelector('canvas')
    if (!canvas) throw new Error('the viewer has no canvas')
    return { width: canvas.width, height: canvas.height }
  },
  container,
  videoElement,
  drag,
  wheel,
  projection: (kind) => projections[kind],
  get drawCount () { return drawCount },
  listenerCount: (target) => tally.get(target) ?? 0,
  liveListenerCount () {
    let total = 0
    for (const target of watched) total += tally.get(target) ?? 0
    return total
  },
  destroyDevice (viewer) {
    const backend = owners.get(viewer)
    if (!backend) throw new Error('this viewer has no WebGPU backend to destroy')
    backend.device.destroy()
  }
}

export default hooks
```

三条约定，都是这个出口存在的理由：

1. **`dpr` 不是库的选项。** 它是页面状态，所以由钩子改 `window.devicePixelRatio`；库读的仍是真值，只是真值被换成了 2。
2. **`readCanvas` 走 2D canvas 的 `drawImage`，不读后端的纹理。** 交换链纹理属于后端，读它要后端的内部 API；canvas 的 image data 是唯一不依赖后端的读法。
3. **`drawCount` 与 `destroyDevice` 都靠包装 `WebGPUBackend` 拿到。** 「dispose 之后不再画」和「设备丢了要上报」这两条，从页面外部没有别的观测手段 —— 一个停了的循环和一个一直画同一帧的循环长得一模一样。这也是这个文件只能待在 `demo/` 的原因：它动了生产类，而 `src/index.ts` 一个内部符号都不挂。

---

### Task 1: `CameraController`

**Files:**
- Create: `src/viewer/types.ts`
- Create: `src/viewer/camera-controller.ts`
- Test: `test/unit/camera-controller.test.ts`

**背景**：P2 的 `matrix.ts` 是**无状态**的（吃 `CameraState` 吐矩阵）。有状态的部分 —— 设值时钳制、`rotate`/`zoom` 的增量语义、dirty 标记 —— 在这里。

`CameraOptions` 也在这里定义：它是**公开 API 的一部分**（`viewer.cameraOptions` 的读写类型、两个公开类的 `camera` 选项），所以它属于 `viewer/`，不属于 `core/` —— `core/` 里的 `CameraState` / `Projection` 是渲染用的数据，而这个是「调用者能填哪些字段」。

- [ ] **Step 1: 写失败测试**

`test/unit/camera-controller.test.ts`：

```ts
import { describe, it, expect, vi } from 'vitest'
import { CameraController } from '../../src/viewer/camera-controller'
import type { Projection } from '../../src/core/types'

// Fully-specified projections: `as const` on a partial literal would look like a
// Projection without being one, and the point of these fixtures is that they are
// actually assignable.
//
// `fov` is in RADIANS. cuon's `mat4.perspective` took degrees and P2's
// `legacyFovFrom` exists to invert that, so a fixture in degrees would be a
// fixture of a different quantity -- 90 would mean 5157 degrees of field of view.
const linear: Projection = { kind: 'linear', fov: Math.PI / 2, aspect: 1 }
const cylindrical: Projection = { kind: 'cylindrical', zoom: 1, extent: [1, 1] }

describe('CameraController', () => {
  it('starts at the given pose', () => {
    const c = new CameraController({ povLatitude: 10, povLongitude: 20 }, linear)
    expect(c.state).toEqual({ povLatitude: 10, povLongitude: 20 })
  })

  it('defaults to the origin when no pose is given', () => {
    const c = new CameraController(undefined, linear)
    expect(c.state).toEqual({ povLatitude: 0, povLongitude: 0 })
  })

  it('clamps latitude instead of wrapping it', () => {
    // Latitude 100 must NOT become -80. Wrapping at the poles puts the viewer
    // past the point where the equirectangular mapping is defined and the
    // matrix goes singular. Longitude wraps; latitude does not.
    const c = new CameraController(undefined, linear)
    c.setPose({ povLatitude: 100, povLongitude: 0 })
    expect(c.state.povLatitude).toBe(90)
    c.setPose({ povLatitude: -100, povLongitude: 0 })
    expect(c.state.povLatitude).toBe(-90)
  })

  it('wraps longitude into [0, 360)', () => {
    const c = new CameraController(undefined, linear)
    c.setPose({ povLatitude: 0, povLongitude: 370 })
    expect(c.state.povLongitude).toBe(10)
    c.setPose({ povLatitude: 0, povLongitude: -10 })
    expect(c.state.povLongitude).toBe(350)
    c.setPose({ povLatitude: 0, povLongitude: 360 })
    expect(c.state.povLongitude).toBe(0)
  })

  it('marks itself clean after read and dirty after a change', () => {
    // The dirty flag is what lets a static image render exactly once instead of
    // sixty times a second forever.
    const c = new CameraController(undefined, linear)
    expect(c.consumeDirty()).toBe(true)   // first frame always draws
    expect(c.consumeDirty()).toBe(false)
    c.rotate(5, 5)
    expect(c.consumeDirty()).toBe(true)
    expect(c.consumeDirty()).toBe(false)
  })

  it('rotate applies a relative delta and accumulates', () => {
    const c = new CameraController(undefined, linear)
    c.rotate(10, 20)
    c.rotate(5, 5)
    expect(c.state).toEqual({ povLatitude: 15, povLongitude: 25 })
  })

  it('rotate clamps and wraps through the same path as setPose', () => {
    const c = new CameraController(undefined, linear)
    c.rotate(200, 400)
    expect(c.state).toEqual({ povLatitude: 90, povLongitude: 40 })
  })

  it('rotate by zero does not dirty the controller', () => {
    // Otherwise every pointermove that did not move anything forces a redraw.
    const c = new CameraController(undefined, linear)
    c.consumeDirty()
    c.rotate(0, 0)
    expect(c.consumeDirty()).toBe(false)
  })

  it('zoom only applies to projections that have one', () => {
    const c = new CameraController(undefined, { kind: 'linear', fov: Math.PI / 2, aspect: 1 })
    c.zoom(0.5)
    expect(c.consumeDirty()).toBe(false)
  })

  it('zoom clamps to the projection range', () => {
    // The discriminant is a string, so the guard in `zoom()` is a real narrowing
    // and not a numeric comparison: a wrong guard would fall through to reading
    // `zoom` off the linear variant, where it does not exist, and the camera
    // would end up with a NaN fov rather than a clamped zoom.
    const c = new CameraController(undefined, cylindrical)
    const zoomOf = (): number => {
      const p = c.projection
      return p.kind === 'cylindrical' ? p.zoom : NaN
    }

    c.zoom(100)
    expect(zoomOf()).toBeLessThanOrEqual(1)
    c.zoom(-100)
    expect(zoomOf()).toBeGreaterThan(0)
  })

  it('replacing the projection marks dirty and does not reset the pose', () => {
    // cameraOptions is a live setter. Swapping the projection must not move the
    // camera -- the legacy version reconstructed the whole camera and silently
    // reset the pose to the origin.
    const c = new CameraController({ povLatitude: 30, povLongitude: 60 }, linear)
    c.consumeDirty()
    c.setProjection(cylindrical)
    expect(c.state).toEqual({ povLatitude: 30, povLongitude: 60 })
    expect(c.consumeDirty()).toBe(true)
  })

  it('notifies subscribers on change but not on a no-op', () => {
    const c = new CameraController(undefined, linear)
    const fn = vi.fn()
    c.onChange(fn)
    c.rotate(1, 1)
    expect(fn).toHaveBeenCalledTimes(1)
    c.rotate(0, 0)
    expect(fn).toHaveBeenCalledTimes(1)
  })

  it('setAspect rewrites the linear projection and marks dirty', () => {
    // The surface's aspect is knowledge only the viewer's resize handler has, and
    // for the linear camera it IS the projection's aspect. Without this the first
    // frame of a 16:9 viewer would be rendered with the default square aspect.
    const c = new CameraController(undefined, linear)
    c.consumeDirty()
    c.setAspect(2)
    expect(c.projection).toEqual({ kind: 'linear', fov: Math.PI / 2, aspect: 2 })
    expect(c.consumeDirty()).toBe(true)
  })

  it('setAspect leaves a non-linear projection alone', () => {
    // The other three cameras get their surface shape from the quad's extent,
    // which the shader reads; there is no aspect field to write and inventing one
    // would be a field nothing consumes.
    const c = new CameraController(undefined, cylindrical)
    c.consumeDirty()
    c.setAspect(2)
    expect(c.projection).toEqual(cylindrical)
    expect(c.consumeDirty()).toBe(false)
  })

  it('setAspect is a no-op when the aspect did not change', () => {
    // ResizeObserver fires on every layout pass, including ones that changed only
    // the position, so an unchanged aspect must not cost a full redraw.
    const c = new CameraController(undefined, linear)
    c.consumeDirty()
    c.setAspect(1)
    expect(c.consumeDirty()).toBe(false)
  })

  it('rejects a non-positive aspect', () => {
    // Reached through `#resize` with `height === 0` if the guard there were ever
    // dropped, and a zero aspect makes the projection matrix singular.
    const c = new CameraController(undefined, linear)
    expect(() => c.setAspect(0)).toThrow(/greater than 0/i)
    expect(() => c.setAspect(-1)).toThrow(/greater than 0/i)
    expect(() => c.setAspect(NaN)).toThrow(/greater than 0/i)
  })

  it('invalidate forces the next frame to draw', () => {
    // Called after a resize reallocated the drawing buffer: the buffer is clear
    // now, so a frame has to be drawn whether or not the camera moved.
    const c = new CameraController(undefined, linear)
    c.consumeDirty()
    c.invalidate()
    expect(c.consumeDirty()).toBe(true)
  })

  it('rejects a non-finite angle rather than producing a NaN matrix', () => {
    // A NaN latitude silently produces a matrix of NaNs, which draws black with
    // no error anywhere. Better to fail at the boundary.
    const c = new CameraController(undefined, linear)
    expect(() => c.setPose({ povLatitude: NaN, povLongitude: 0 })).toThrow(/finite/i)
    expect(() => c.rotate(0, Infinity)).toThrow(/finite/i)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test:unit -- camera-controller`
Expected: FAIL —— 无法解析 `../../src/viewer/camera-controller`

- [ ] **Step 3: 实现**

`src/viewer/types.ts`：

```ts
/**
 * The camera half of the public options.
 *
 * `pose` is a partial because the common case is "just move the projection" or
 * "just start at this angle", and requiring both halves would force callers to
 * restate the default. Omitting it means "keep the current pose", which is what
 * makes the `cameraOptions` setter usable for a live projection swap -- see
 * `CameraController.setProjection`.
 *
 * `projection` is required: there is no sensible default that is not also a
 * silent choice of rendering model.
 */

import type { CameraState, Projection } from '../core/types'

export interface CameraOptions {
  /** Initial or replacement pose, in degrees. Omitted means "unchanged". */
  readonly pose?: Partial<CameraState>
  readonly projection: Projection
}
```

`src/viewer/camera-controller.ts`：

```ts
/**
 * The single owner of camera state.
 *
 * The matrix maths is stateless and lives in `core/matrix.ts`. What is here is
 * everything that needs memory: clamping on assignment, the relative semantics
 * of rotate and zoom, and the dirty flag.
 *
 * The dirty flag is why a static image renders once instead of sixty times a
 * second. The legacy FrameDriver redrew unconditionally on every rAF tick,
 * which is a full-screen fragment shader running forever for a picture that
 * has not changed.
 *
 * Nothing here recomputes a projection formula. The CPU-side authority for those
 * is `core/reference.ts` (`project` / `ndcToSurface`), which exists so that a
 * test can predict what the shader will do; duplicating a formula here would
 * create a second opinion about it.
 */

import type { CameraState, Projection } from '../core/types'
import { clampLatitude, wrapLongitude, assertFinite, assertPositive } from '../core/validate'

/**
 * The camera a viewer starts with when the caller does not name one.
 *
 * The legacy value: `CameraFactory.defaultDataMap.perspective` was
 * `{ fov: 70, aspect: 1 }`, in degrees because cuon's `mat4.perspective` took
 * degrees. `Projection.fov` is in radians (P2's `legacyFovFrom` inverts the
 * degree-based matrix, which is what proves it), so 70 degrees is converted here
 * once rather than at every call site.
 *
 * `aspect` is a placeholder: the viewer overwrites it with the surface's real
 * aspect on the first resize, which happens during construction. It cannot be
 * known before a container exists, and 1 is the value that is wrong in the least
 * visible way if a frame somehow got drawn first.
 */
export const DEFAULT_PROJECTION: Projection = {
  kind: 'linear',
  fov: (70 * Math.PI) / 180,
  aspect: 1
}

export class CameraController {
  #state: CameraState
  #projection: Projection
  #dirty = true
  readonly #changeListeners = new Set<() => void>()

  /**
   * @param pose - Initial pose in degrees. Defaults to the origin.
   * @param projection - Initial projection.
   */
  constructor (pose: Partial<CameraState> | undefined, projection: Projection) {
    this.#state = {
      povLatitude: clampLatitude(pose?.povLatitude ?? 0),
      povLongitude: wrapLongitude(pose?.povLongitude ?? 0)
    }
    this.#projection = projection
  }

  /** The current pose. */
  get state (): CameraState { return this.#state }

  /** The current projection. */
  get projection (): Projection { return this.#projection }

  /**
   * Reads and clears the dirty flag.
   *
   * Consuming rather than reading is deliberate: the renderer is the only
   * consumer, and a flag that had to be cleared by hand is exactly the kind of
   * state that goes wrong. The legacy `needUpdate_` latch was cleared in one
   * place and set in three.
   */
  consumeDirty (): boolean {
    const was = this.#dirty
    this.#dirty = false
    return was
  }

  /**
   * Sets the pose.
   *
   * Latitude is clamped to [-90, 90]; longitude wraps into [0, 360). The two
   * are different on purpose -- wrapping latitude would carry the viewer past
   * the pole to the mirrored side, where the equirectangular mapping is
   * singular.
   *
   * @throws If either angle is not finite. An infinite or NaN angle produces a
   *   matrix of NaNs that draws a black frame with no error reported anywhere.
   */
  setPose (pose: CameraState): void {
    assertFinite(pose.povLatitude, 'povLatitude')
    assertFinite(pose.povLongitude, 'povLongitude')
    this.#apply(pose.povLatitude, pose.povLongitude)
  }

  /**
   * Rotates by a relative delta in degrees.
   *
   * A zero delta is a no-op that does not dirty the controller: every
   * pointermove that did not actually move would otherwise force a full redraw.
   */
  rotate (deltaLat: number, deltaLng: number): void {
    assertFinite(deltaLat, 'deltaLat')
    assertFinite(deltaLng, 'deltaLng')
    if (deltaLat === 0 && deltaLng === 0) return
    this.#apply(this.#state.povLatitude + deltaLat, this.#state.povLongitude + deltaLng)
  }

  /** Changes the zoom of the current projection. No-op for the linear one. */
  zoom (delta: number): void {
    assertFinite(delta, 'zoom delta')
    if (delta === 0) return
    if (this.#projection.kind === 'linear') return
    const next = Math.min(1, Math.max(0.01, this.#projection.zoom * (1 + delta)))
    this.#projection = { ...this.#projection, zoom: next }
    this.#dirty = true
    this.#notify()
  }

  /**
   * Replaces the projection, keeping the pose.
   *
   * The legacy `cameraOptions` setter rebuilt the whole camera, which reset the
   * pose to the origin -- so changing the projection silently threw away where
   * the user was looking.
   */
  setProjection (projection: Projection): void {
    this.#projection = projection
    this.#dirty = true
    this.#notify()
  }

  /**
   * Sets the linear projection's aspect from the surface's, and does nothing to
   * the others.
   *
   * The surface size is knowledge only the viewer's resize handler has, and for
   * the linear camera the projection's aspect IS the surface's -- rendering a
   * 16:9 container with the default square aspect stretches the image. The other
   * three cameras take their shape from the quad's extent, which the shader reads
   * (P2's `LEGACY_EXTENT`), so they have no aspect field to write and are left
   * untouched rather than given one nothing consumes.
   *
   * @param aspect - width / height of the drawing surface. Must be positive.
   * @throws If `aspect` is not finite and greater than zero. A zero aspect makes
   *   the projection matrix singular, which draws nothing and reports nothing.
   */
  setAspect (aspect: number): void {
    assertPositive(aspect, 'aspect')
    if (this.#projection.kind !== 'linear') return
    if (this.#projection.aspect === aspect) return
    this.#projection = { ...this.#projection, aspect }
    this.#dirty = true
    this.#notify()
  }

  /**
   * Marks the next frame as needing a draw, without changing anything.
   *
   * Called after a resize: reallocating the drawing buffer clears it, so the next
   * frame has to be drawn even though the camera did not move. Without this the
   * canvas stays blank until the user happens to rotate something.
   */
  invalidate (): void {
    this.#dirty = true
  }

  /** Subscribes to changes. Returns an unsubscribe function. */
  onChange (fn: () => void): () => void {
    this.#changeListeners.add(fn)
    return () => { this.#changeListeners.delete(fn) }
  }

  #apply (lat: number, lng: number): void {
    const next = { povLatitude: clampLatitude(lat), povLongitude: wrapLongitude(lng) }
    if (next.povLatitude === this.#state.povLatitude && next.povLongitude === this.#state.povLongitude) {
      return
    }
    this.#state = next
    this.#dirty = true
    this.#notify()
  }

  #notify (): void {
    for (const fn of [...this.#changeListeners]) fn()
  }
}
```

> **import 全部来自 P2 的具体模块**：类型（`CameraState` / `Projection`）来自 `core/types.ts`，范围断言（`clampLatitude` / `wrapLongitude` / `assertFinite` / `assertPositive`）来自 `core/validate.ts`。P2 没有 `src/core/index.ts` 这个汇总出口，**本计划也不建**：barrel 会把「哪一层依赖哪一层」藏起来，而这一层的依赖方向恰恰是设计里最要紧的东西。

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test:unit -- camera-controller`
Expected: 17 个测试 PASS

- [ ] **Step 5: Commit**

```bash
git add src/viewer/types.ts src/viewer/camera-controller.ts test/unit/camera-controller.test.ts
git commit -m "feat(viewer): stateful camera controller with clamping and a consumable dirty flag"
```

---

### Task 2: `RenderLoop`

**Files:**
- Create: `src/viewer/render-loop.ts`
- Test: `test/unit/render-loop.test.ts`

**两条必须写进代码的约束：**

1. **脏检查发生在 `getCurrentTexture()` 之前。** WebGPU 里取了交换链纹理却不提交是 **validation error**。旧代码没有这个问题（WebGL 随便画），所以这是个新引入的陷阱。
2. **上限不再是「每秒最多 60 次重绘」，而是「没有变化就一次都不画」。** 旧 `MAX_FRAME_RATE = 60` 节流的是无条件重绘。

- [ ] **Step 1: 写失败测试**

`test/unit/render-loop.test.ts`：

```ts
import { describe, it, expect, vi } from 'vitest'
import { RenderLoop } from '../../src/viewer/render-loop'

/** A controllable rAF stand-in. */
function fakeScheduler () {
  const queue = new Map<number, FrameRequestCallback>()
  let next = 1
  return {
    request: (cb: FrameRequestCallback) => { const id = next++; queue.set(id, cb); return id },
    cancel: (id: number) => { queue.delete(id) },
    /** Runs one tick at the given timestamp. */
    tick (time: number) {
      const pending = [...queue.entries()]
      queue.clear()
      for (const [, cb] of pending) cb(time)
    },
    get pending () { return queue.size }
  }
}

describe('RenderLoop', () => {
  it('draws the first frame even when nothing is dirty', () => {
    // The dirty flag starts clean after construction in some orderings; a loop
    // that skipped the first frame would show nothing until the user moved.
    const s = fakeScheduler()
    const draw = vi.fn()
    const loop = new RenderLoop({ schedule: s.request, cancel: s.cancel, shouldDraw: () => false, draw })
    loop.start()
    s.tick(0)
    expect(draw).toHaveBeenCalledTimes(1)
  })

  it('skips drawing when nothing changed', () => {
    const s = fakeScheduler()
    const draw = vi.fn()
    let dirty = true
    const loop = new RenderLoop({ schedule: s.request, cancel: s.cancel, shouldDraw: () => dirty, draw })
    loop.start()
    s.tick(0); dirty = false
    s.tick(16)
    s.tick(32)
    expect(draw).toHaveBeenCalledTimes(1)
  })

  it('asks shouldDraw before drawing, every tick', () => {
    // The contract that keeps getCurrentTexture() out of a no-op frame. If the
    // loop ever calls draw() first and checks afterwards, a dropped frame is a
    // WebGPU validation error rather than a wasted draw.
    const s = fakeScheduler()
    const calls: string[] = []
    const loop = new RenderLoop({
      schedule: s.request,
      cancel: s.cancel,
      shouldDraw: () => { calls.push('check'); return true },
      draw: () => { calls.push('draw') }
    })
    loop.start()
    s.tick(0)
    expect(calls).toEqual(['check', 'draw'])
  })

  it('stops scheduling after stop()', () => {
    const s = fakeScheduler()
    const loop = new RenderLoop({ schedule: s.request, cancel: s.cancel, shouldDraw: () => true, draw: vi.fn() })
    loop.start()
    s.tick(0)
    loop.stop()
    expect(s.pending).toBe(0)
  })

  it('a throw inside draw does not kill the loop', () => {
    // A device-lost mid-frame would otherwise stop the loop silently and the
    // canvas would freeze with no further events.
    const s = fakeScheduler()
    const onError = vi.fn()
    let calls = 0
    const loop = new RenderLoop({
      schedule: s.request,
      cancel: s.cancel,
      shouldDraw: () => true,
      draw: () => { calls++; if (calls === 1) throw new Error('device lost') },
      onError
    })
    loop.start()
    s.tick(0)
    s.tick(16)
    expect(onError).toHaveBeenCalledTimes(1)
    expect(calls).toBe(2)
  })

  it('stop() then start() resumes', () => {
    const s = fakeScheduler()
    const draw = vi.fn()
    const loop = new RenderLoop({ schedule: s.request, cancel: s.cancel, shouldDraw: () => true, draw })
    loop.start()
    s.tick(0)
    loop.stop()
    loop.start()
    s.tick(16)
    expect(draw).toHaveBeenCalledTimes(2)
  })

  it('dispose stops the loop and is idempotent', () => {
    const s = fakeScheduler()
    const loop = new RenderLoop({ schedule: s.request, cancel: s.cancel, shouldDraw: () => true, draw: vi.fn() })
    loop.start()
    loop.dispose()
    loop.dispose()
    expect(s.pending).toBe(0)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test:unit -- render-loop`
Expected: FAIL —— 无法解析 `../../src/viewer/render-loop`

- [ ] **Step 3: 实现**

`src/viewer/render-loop.ts`：

```ts
/**
 * The animation frame loop.
 *
 * Two things about this differ from the legacy FrameDriver:
 *
 * 1. It does not redraw unconditionally. The legacy loop ran a full-screen
 *    fragment shader sixty times a second forever, including for a still image
 *    that had not changed since it loaded. Here `shouldDraw` decides, and a
 *    static image renders exactly once.
 *
 * 2. `shouldDraw` is asked BEFORE the frame is drawn. That ordering is a
 *    hard requirement on WebGPU: acquiring the swapchain texture and then not
 *    submitting a command buffer is a validation error. Callers implement
 *    `shouldDraw` so that acquiring happens inside `draw`, after the check.
 */

import { Disposable } from '../core/events'

export interface RenderLoopOptions {
  /** `requestAnimationFrame`. Injected so the loop is testable without a DOM. */
  readonly schedule: (cb: FrameRequestCallback) => number
  /** `cancelAnimationFrame`. */
  readonly cancel: (handle: number) => void
  /**
   * Whether this frame needs drawing. Called once per tick, before `draw`.
   *
   * Implementations should consume their dirty state here, not in `draw`.
   */
  readonly shouldDraw: () => boolean
  readonly draw: () => void
  /** Called when `draw` throws. The loop keeps running. */
  readonly onError?: (error: unknown) => void
}

export class RenderLoop extends Disposable {
  readonly #options: RenderLoopOptions
  #handle: number | undefined
  #running = false
  #drewOnce = false

  constructor (options: RenderLoopOptions) {
    super()
    this.#options = options
  }

  /** Starts the loop. Harmless if already running. */
  start (): void {
    if (this.#running || this.isDisposed) return
    this.#running = true
    this.#scheduleNext()
  }

  /** Stops the loop, leaving it restartable. */
  stop (): void {
    this.#running = false
    if (this.#handle !== undefined) {
      this.#options.cancel(this.#handle)
      this.#handle = undefined
    }
  }

  #scheduleNext (): void {
    this.#handle = this.#options.schedule(() => {
      this.#handle = undefined
      if (!this.#running) return
      this.#tick()
      if (this.#running) this.#scheduleNext()
    })
  }

  #tick (): void {
    // The first frame draws unconditionally: a loop whose dirty state started
    // false would show nothing at all until the user interacted.
    const first = !this.#drewOnce
    if (!first && !this.#options.shouldDraw()) return

    try {
      this.#options.draw()
      this.#drewOnce = true
    } catch (error) {
      // Deliberately swallowed. A device-lost mid-frame would otherwise stop
      // the loop silently, and the canvas would freeze with no event and no
      // further attempt -- the exact failure the legacy code had, where a
      // context loss produced a permanently black canvas.
      this.#options.onError?.(error)
    }
  }

  override dispose (): void {
    if (this.isDisposed) return
    this.stop()
    super.dispose()
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npm run test:unit -- render-loop`
Expected: 7 个测试 PASS

- [ ] **Step 5: Commit**

```bash
git add src/viewer/render-loop.ts test/unit/render-loop.test.ts
git commit -m "feat(viewer): a render loop that draws only when something changed

The legacy FrameDriver redrew unconditionally at up to 60fps, including for
a still image. shouldDraw is consulted before draw so that a skipped frame
never acquires the swapchain texture -- which on WebGPU is a validation
error rather than a wasted call."
```

---

### Task 3: `Viewer` 组装与 `dispose` 顺序

**Files:**
- Create: `src/viewer/viewer.ts`
- Create: `src/viewer/backend-factory.ts`
- Test: `test/integration/dispose-order.test.ts`

- [ ] **Step 1: 实现 `backend-factory.ts`**

```ts
/**
 * Picking a backend, and reporting which one was picked.
 *
 * Falling back is a programmable state rather than a log line, so an
 * application can show "your browser is using the slower renderer" or report
 * it. See `Capabilities`.
 */

import type { Backend } from '../renderer/backend'
import { describeCapabilities, type ProbeInput, type SelectedCapabilities } from '../renderer/capabilities'
import { WebGPUBackend } from '../renderer/webgpu/backend'

/**
 * Probes the device without constructing a viewer.
 *
 * Existing because an application needs to decide what to do before it has a
 * container to render into -- and because a viewer that silently downgraded
 * with no way to ask would be the legacy `createProgram` failure again, in a
 * new place.
 *
 * `SelectedCapabilities` rather than `Capabilities`: "there is no backend at
 * all" is a state a caller has to be able to see and act on, and it is not a
 * capability of anything. Throwing instead would turn the most reasonable use
 * of this function -- ask first, then decide -- into a try/catch.
 */
export async function probe (): Promise<SelectedCapabilities> {
  const hasWebGPU = typeof navigator !== 'undefined' && 'gpu' in navigator
  let adapter: Record<string, string> | null = null
  let maxTextureDimension = 0

  if (hasWebGPU) {
    const a = await navigator.gpu.requestAdapter()
    if (a) {
      adapter = a.info as unknown as Record<string, string>
      maxTextureDimension = a.limits.maxTextureDimension2D
    }
  }

  const hasWebGL2 = typeof document !== 'undefined' &&
    document.createElement('canvas').getContext('webgl2') !== null

  return describeCapabilities({
    hasWebGPU,
    adapter,
    hasWebGL2,
    maxTextureDimension,
    externalTextures: adapter !== null
  } satisfies ProbeInput)
}

/**
 * Creates a backend for the given canvas.
 *
 * @throws If neither backend is available. Constructing a viewer that can never
 *   draw is what the legacy `createProgram` did -- it logged and returned null,
 *   and the viewer reported success. A viewer that cannot render is not a
 *   viewer.
 */
export async function createBackend (canvas: HTMLCanvasElement): Promise<Backend> {
  const webgpu = await WebGPUBackend.create(canvas)
  if (webgpu) return webgpu

  // The WebGL2 backend arrives in P6. Until then, a page without WebGPU cannot
  // be served, and saying so is better than a black rectangle.
  throw new Error(
    'no usable rendering backend: WebGPU is unavailable and the WebGL2 fallback is not implemented yet'
  )
}
```

- [ ] **Step 2: 实现 `viewer.ts`**

```ts
/**
 * The shared viewer, composed rather than mixed in.
 *
 * The legacy version was `mix(Viewer).with(CameraFactory)` where `Viewer` was
 * itself `mix(Eventable).with(Delegate, RenderFlow)`. The problem was not the
 * syntax but the semantics: the viewer WAS a Delegate and WAS a RenderFlow, so
 * every member landed in one namespace, initialisation order was decided
 * implicitly by mixin order, and `dispose` chained through `super` -- which is
 * why five separate teardown steps were missing.
 *
 * Here each collaborator is a field and `dispose` names them in order.
 */

import { Disposable, EventEmitter, type EventMap } from '../core/events'
import { channels } from '../diagnostics'
import type { Backend, Capabilities, DeviceLost } from '../renderer/backend'
import type { MediaSource } from '../media/source'
import { CameraController, DEFAULT_PROJECTION } from './camera-controller'
import { InputController } from '../interaction/input-controller'
import { RenderLoop } from './render-loop'
import type { CameraOptions } from './types'

/**
 * Everything the viewer can emit.
 *
 * The eight `media-*` events are the source's, re-emitted with `target` rewritten
 * to the viewer. They are declared here rather than left to the source's own
 * `MediaEvents`, because a consumer holds a viewer and never a source:
 * `viewer.on('media-play', ...)` has to typecheck against the payload the viewer
 * actually hands out, which is `{ target: Viewer }`.
 */
export interface ViewerEvents extends EventMap {
  rotate: { lat: number, lng: number }
  zoom: { delta: number }
  'device-lost': DeviceLost
  'media-load': { target: Viewer }
  'media-error': { target: Viewer, error: unknown }
  'media-play': { target: Viewer }
  'media-pause': { target: Viewer }
  'media-ended': { target: Viewer }
  'media-seeking': { target: Viewer }
  'media-seeked': { target: Viewer }
  'media-progress': { target: Viewer }
}

/** What the constructor needs. Subclasses assemble it; nobody else does. */
export interface ViewerInit {
  /** The element the canvas is appended to. */
  readonly container: HTMLElement
  /**
   * The canvas to draw into.
   *
   * Created by the caller, not here, because acquiring a GPU device needs a
   * canvas and is asynchronous: it has to exist before this constructor runs, and
   * the backend was built from it. Ownership transfers at construction -- the
   * viewer appends it and removes it on dispose, so there is exactly one canvas
   * and the one the backend draws into is the one in the DOM. The legacy viewer
   * built its own canvas internally, which is why `renderer-canvas` (the class
   * its gesture callbacks were bound to) was not the canvas anybody was drawing
   * into.
   */
  readonly canvas: HTMLCanvasElement
  readonly camera: CameraOptions | undefined
  readonly backend: Backend
}

export class Viewer extends Disposable {
  protected readonly events = new EventEmitter<ViewerEvents>()
  readonly #camera: CameraController
  readonly #input: InputController
  readonly #backend: Backend
  readonly #loop: RenderLoop
  protected readonly canvas: HTMLCanvasElement
  /** The current source. Assigned through `setSource`, never directly. */
  protected source: MediaSource | undefined
  #sourceOffs: Array<() => void> = []
  #offDeviceLost: (() => void) | undefined
  #lastSourceVersion = -1
  #disposing = false

  constructor (init: ViewerInit) {
    super()
    this.canvas = init.canvas
    this.canvas.className = 'pano-canvas'
    Object.assign(this.canvas.style, {
      display: 'block',
      width: '100%',
      height: '100%'
    })
    init.container.appendChild(this.canvas)

    this.#backend = init.backend
    this.#camera = new CameraController(init.camera?.pose, init.camera?.projection ?? DEFAULT_PROJECTION)
    this.#input = new InputController(this.canvas)

    this.#loop = new RenderLoop({
      schedule: (cb) => window.requestAnimationFrame(cb),
      cancel: (h) => window.cancelAnimationFrame(h),
      // Consuming the dirty flags HERE, before draw() acquires the swapchain
      // texture. On WebGPU, acquiring and not submitting is a validation error.
      shouldDraw: () => this.#camera.consumeDirty() || this.#sourceChanged(),
      draw: () => this.#drawFrame(),
      // A trace channel, not `console.error`. An application that wants to report
      // a failed frame reads the channel; one that does not is not shouted at.
      // Spec 7.3: results travel as events, process as debug.
      onError: (error) => { channels.viewer('frame failed %o', error) }
    })

    // The only channel through which a lost device can be reported. Registered
    // before the first frame -- a device can go away at any time, including
    // during startup.
    this.#offDeviceLost = this.#backend.onDeviceLost((lost) => {
      // Emitted BEFORE the teardown below, and that order is the point: a
      // consumer that learns about the loss can still read the viewer, and a
      // listener that calls `dispose()` itself finds the work already done
      // instead of re-entering it (`#disposing`).
      this.events.emit('device-lost', lost)
      this.dispose()
    })

    this.#input.on('pan', ({ deltaX, deltaY }) => {
      const d = this.#input.dragToRotation(deltaX, deltaY, {
        width: this.canvas.clientWidth,
        height: this.canvas.clientHeight
      })
      if (d.lat === 0 && d.lng === 0) return
      this.#camera.rotate(d.lat, d.lng)
      this.events.emit('rotate', { lat: d.lat, lng: d.lng })
    })
    this.#input.on('zoom', ({ delta }) => {
      this.#camera.zoom(delta)
      this.events.emit('zoom', { delta })
    })

    this.#resize()
    this.#observeResize()
    this.#loop.start()
  }

  /**
   * Subscribes to viewer events. Returns an unsubscribe function.
   *
   * `'*'` receives every event as `(type, event)` -- the typed form of the legacy
   * `trigger('*')`.
   */
  on<K extends keyof ViewerEvents & string> (type: K, fn: (event: ViewerEvents[K]) => void): () => void {
    return this.events.on(type, fn)
  }

  /** The device's real limits, as reported by the backend in use. */
  get capabilities (): Capabilities {
    return this.#backend.capabilities
  }

  get PTZ (): boolean { return this.#input.PTZ }
  set PTZ (value: boolean) {
    this.assertAlive()
    this.#input.PTZ = value
  }

  get cameraOptions (): CameraOptions {
    return { pose: this.#camera.state, projection: this.#camera.projection }
  }

  set cameraOptions (options: CameraOptions) {
    this.assertAlive()
    // Only the projection is replaced; the pose is kept. The legacy setter
    // rebuilt the whole camera and reset it to the origin, so changing the
    // projection silently threw away where the user was looking. A caller that
    // wants to move the camera sets `pose` through `rotate` or `setPose`, which
    // is where the clamping lives.
    if (options.pose) this.#camera.setPose({ ...this.#camera.state, ...options.pose })
    this.#camera.setProjection(options.projection)
  }

  /** Rotates the camera. Public so applications can drive it programmatically. */
  rotate (lat: number, lng: number): void {
    this.assertAlive()
    this.#camera.rotate(lat, lng)
  }

  /** Zooms. No-op for the linear projection, which has no zoom. */
  zoom (delta: number): void {
    this.assertAlive()
    this.#camera.zoom(delta)
  }

  /**
   * Installs a source, replacing any previous one.
   *
   * Written once, here, because a source swap has four parts that must happen
   * together: drop the old subscriptions, dispose the old source, subscribe the
   * new one, and reset the version latch. A subclass that did its own swap would
   * get one of them wrong, and the one it would get wrong is the version latch --
   * which shows up as "the new image never appears", not as an error.
   *
   * @param source - The new source, or `undefined` to detach (what `dispose`
   *   does). A detached viewer draws the background.
   */
  protected setSource (source: MediaSource | undefined): void {
    for (const off of this.#sourceOffs) off()
    this.#sourceOffs = []
    this.source?.dispose()
    this.source = source
    // A new source starts its own version counter at 0, so the latch has to go
    // back to a value no source can produce. Otherwise a swap to a source that
    // has not advanced yet reads as "unchanged" and the canvas keeps the old
    // image.
    this.#lastSourceVersion = -1
    if (source) this.#sourceOffs = this.#forwardMediaEvents(source)
  }

  /**
   * Re-emits a source's media events as the viewer's own.
   *
   * Eight explicit lines rather than a loop over the event names: a loop types
   * the name as a union of all eight, which destroys the emitter's
   * one-name-to-one-payload correlation, and every line would then need a cast.
   * Eight lines is the cheaper half of that trade, and a cast here would quietly
   * accept a payload that does not match the event.
   *
   * `target` is rewritten to the viewer because the source is an implementation
   * detail: an event pointing at it would hand the application an object it was
   * never given.
   */
  #forwardMediaEvents (source: MediaSource): Array<() => void> {
    return [
      source.on('media-load', () => { this.events.emit('media-load', { target: this }) }),
      source.on('media-error', (e) => { this.events.emit('media-error', { target: this, error: e.error }) }),
      source.on('media-play', () => { this.events.emit('media-play', { target: this }) }),
      source.on('media-pause', () => { this.events.emit('media-pause', { target: this }) }),
      source.on('media-ended', () => { this.events.emit('media-ended', { target: this }) }),
      source.on('media-seeking', () => { this.events.emit('media-seeking', { target: this }) }),
      source.on('media-seeked', () => { this.events.emit('media-seeked', { target: this }) }),
      source.on('media-progress', () => { this.events.emit('media-progress', { target: this }) })
    ]
  }

  #resizeObserver: ResizeObserver | undefined

  #observeResize (): void {
    // ResizeObserver rather than a window resize listener: a viewer inside a
    // flex layout or a resizable panel changes size without the window doing so.
    // The legacy code listened on window and additionally called
    // `window.removeEventLstener` -- a typo, so its listener was never removed.
    this.#resizeObserver = new ResizeObserver(() => this.#resize())
    this.#resizeObserver.observe(this.canvas)
  }

  #resize (): void {
    const width = this.canvas.clientWidth
    const height = this.canvas.clientHeight
    this.#backend.resize(width, height, window.devicePixelRatio || 1)

    // The resize reallocated the drawing buffer, which clears it, so the next
    // frame has to be drawn whether or not the camera changed -- and the linear
    // projection's aspect IS this surface's, which is knowledge only this method
    // has (see `CameraController.setAspect`).
    this.#camera.invalidate()
    if (height > 0) this.#camera.setAspect(width / height)
  }

  #sourceChanged (): boolean {
    if (!this.source) return false
    try {
      const version = this.source.frame.version
      if (version === this.#lastSourceVersion) return false
      this.#lastSourceVersion = version
      return true
    } catch {
      // A source whose metadata has not loaded throws on `frame`. That is not an
      // error condition for the loop -- there is simply nothing to draw yet.
      return false
    }
  }

  #drawFrame (): void {
    const source = this.source
    if (source) {
      // Read the frame fresh every frame. An external texture is destroyed at
      // the end of the task that created it, so a cached snapshot is a
      // use-after-free rather than a stale frame. The frame object is handed to
      // the backend as-is: `MediaFrame` and P3's `RenderableSource` have the same
      // fields by construction, so there is no adapter to drift.
      try {
        this.#backend.setSource(source.frame)
      } catch {
        // Metadata has not loaded yet, so there is no upload description. Not an
        // error: drawing nothing is the correct frame.
        this.#backend.setSource(null)
      }
    } else {
      this.#backend.setSource(null)
    }
    this.#backend.setCamera(this.#camera.state, this.#camera.projection)
    this.#backend.render()
    // After the draw, not before: `version` has to describe the frame just drawn
    // while the backend is reading it. A video advances its version here, which
    // is what makes the next frame draw at all; an image ignores it.
    source?.markFramePresented()
  }

  /**
   * Releases everything, in order.
   *
   * Order matters and is the reason this is written out rather than inherited:
   * stop the producer before dismantling the consumer. The loop must die first,
   * or a frame in flight reaches a destroyed backend.
   *
   * Idempotent, and re-entrant-safe: the device-lost observer calls this from
   * inside a backend callback, and `#disposing` is what keeps that from running
   * the teardown twice before `super.dispose()` has marked the object disposed.
   */
  override dispose (): void {
    if (this.#disposing) return
    this.#disposing = true

    this.#loop.dispose()            // 1. stop producing frames
    this.#offDeviceLost?.()         // 2. stop accepting device-loss reports
    this.#input.dispose()           // 3. stop producing gestures
    this.setSource(undefined)       // 4. stop producing source changes
    this.#backend.dispose()         // 5. tear down the consumer
    this.#resizeObserver?.disconnect()
    this.events.removeAllListeners()
    this.canvas.remove()
    super.dispose()
  }
}
```

- [ ] **Step 3: 写 dispose 顺序的集成测试**

`test/integration/dispose-order.test.ts`：

```ts
import { test, expect } from './support/fixtures'

/*
 * The legacy codebase leaked in five places, and none of them were "somebody
 * forgot to write dispose". Each was a teardown step that existed somewhere in
 * a super chain and was invisible from the class being disposed. These tests
 * assert the observable consequences rather than the call order, so they keep
 * working if the internals are reorganised.
 */
test('after dispose, no frame is drawn and no listener remains', async ({ gpuPage }) => {
  const result = await gpuPage.evaluate(async () => {
    const t = window.__panoTest
    const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
    await t.waitForFrames(2)
    const before = t.drawCount
    viewer.dispose()
    await t.waitForFrames(5)
    return { before, after: t.drawCount, listeners: t.liveListenerCount() }
  })
  expect(result.before).toBeGreaterThan(0)
  expect(result.after).toBe(result.before)
  expect(result.listeners).toBe(0)
})

test('dispose is idempotent', async ({ gpuPage }) => {
  const result = await gpuPage.evaluate(async () => {
    const t = window.__panoTest
    const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
    viewer.dispose()
    let threw = false
    try { viewer.dispose() } catch { threw = true }
    return { threw, disposed: viewer.isDisposed }
  })
  expect(result.threw).toBe(false)
  expect(result.disposed).toBe(true)
})

test('a static image renders a bounded number of frames, not one per rAF', async ({ gpuPage }) => {
  // The legacy FrameDriver redrew unconditionally at up to 60fps. Measured over
  // a second of wall clock, a still image must not produce 60 draws.
  const result = await gpuPage.evaluate(async () => {
    const t = window.__panoTest
    const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
    await t.waitForFrames(3)
    const start = t.drawCount
    await new Promise(r => setTimeout(r, 1000))
    const end = t.drawCount
    viewer.dispose()
    return { frames: end - start }
  })
  expect(result.frames).toBeLessThan(10)
})

test('the public methods throw after dispose rather than drawing into nothing', async ({ gpuPage }) => {
  const result = await gpuPage.evaluate(async () => {
    const t = window.__panoTest
    const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
    viewer.dispose()
    let message = ''
    try { viewer.rotate(10, 10) } catch (e) { message = String(e) }
    return message
  })
  expect(result).toMatch(/disposed/i)
})

test('a destroyed device is reported to listeners and then released', async ({ gpuPage }) => {
  // A real loss, not a simulated one: `device.destroy()` resolves `device.lost`
  // with reason 'destroyed' (spec 11 measured that it does, which is why this is
  // testable at all). `t.destroyDevice` reaches the device through the backend
  // P3 exposes for exactly this kind of reason.
  const result = await gpuPage.evaluate(async () => {
    const t = window.__panoTest
    const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
    const seen: Array<{ reason: string, disposedWhenNotified: boolean }> = []
    viewer.on('device-lost', (lost) => {
      // Read the viewer from inside the handler. Reporting before tearing down is
      // what makes that possible at all, and the dispose here is the re-entrancy
      // case: a listener that cleans up on its own must not run the teardown
      // twice, nor re-enter it halfway through.
      seen.push({ reason: lost.reason, disposedWhenNotified: viewer.isDisposed })
      viewer.dispose()
    })
    t.destroyDevice(viewer)
    // `device.lost` resolves on a later task, so the report cannot be awaited.
    await new Promise(r => setTimeout(r, 100))
    return { seen, disposed: viewer.isDisposed }
  })
  expect(result.seen).toEqual([{ reason: 'destroyed', disposedWhenNotified: false }])
  expect(result.disposed).toBe(true)
})
```

- [ ] **Step 4: 跑测试**

Run: `npm run test:integration -- dispose-order`
Expected: 5 个测试 PASS

- [ ] **Step 5: Commit**

```bash
git add src/viewer/viewer.ts src/viewer/backend-factory.ts test/integration/dispose-order.test.ts
git commit -m "feat(viewer): compose collaborators explicitly and name the teardown order

The legacy five leaks were not forgotten dispose calls -- each was a step
that existed in a super chain and was invisible from the class being
disposed. ResizeObserver replaces the window listener the legacy code never
removed, because it misspelled removeEventListener."
```

---

### Task 4: 两个公开类与 `index.ts`

**Files:**
- Create: `src/viewer/image-viewer.ts`
- Create: `src/viewer/video-viewer.ts`
- Create: `src/index.ts`
- Test: `test/unit/constructor-validation.test.ts`

- [ ] **Step 1: 写构造校验的单元测试**

`test/unit/constructor-validation.test.ts`：

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { validateImageOptions, validateVideoOptions } from '../../src/viewer/options'

describe('image viewer options', () => {
  const container = { nodeType: 1 } as unknown as HTMLElement

  it('accepts the minimum', () => {
    expect(() => validateImageOptions({ container, src: '/a.png' })).not.toThrow()
  })

  it('rejects a missing container with a message that names the field', () => {
    expect(() => validateImageOptions({ src: '/a.png' } as never))
      .toThrow(/container/)
  })

  it('rejects a non-string src', () => {
    expect(() => validateImageOptions({ container, src: 42 } as never)).toThrow(/src/)
  })

  it('rejects an empty src', () => {
    // An empty src silently loads the current page as an image, which then
    // fails to decode, and the error surfaces as a media-error with no hint
    // that the URL was the empty string.
    expect(() => validateImageOptions({ container, src: '' })).toThrow(/src/)
  })

  it('accepts the corrected spelling of the projection', () => {
    expect(() => validateImageOptions({ container, src: '/a.png', projection: 'equirectangular' }))
      .not.toThrow()
  })

  it('rejects the legacy misspelling with a message that says what to use', () => {
    // "equiprectangular" was the shipped spelling. Failing with the correction
    // in the message is the difference between a five-second fix and a
    // confusing "invalid projection" when upgrading.
    expect(() => validateImageOptions({ container, src: '/a.png', projection: 'equiprectangular' } as never))
      .toThrow(/equirectangular/)
  })

  it('rejects fisheye at construction rather than at render time', () => {
    // The legacy code accepted 'fisheye' and threw from inside the texture
    // update, so the viewer constructed "successfully" and then failed on the
    // first frame. Construct-time is the only useful time to fail.
    expect(() => validateImageOptions({ container, src: '/a.png', projection: 'fisheye' }))
      .toThrow(/fisheye.*not implemented/i)
  })

  it('rejects the removed frameSize option with an explanation', () => {
    // Silently ignoring it would leave an upgrader believing their oversized
    // image is still being downscaled by that setting.
    expect(() => validateImageOptions({ container, src: '/a.png', frameSize: [2048, 1024] } as never))
      .toThrow(/frameSize.*removed/i)
  })
})

describe('video viewer options', () => {
  const container = { nodeType: 1 } as unknown as HTMLElement

  it('accepts the minimum', () => {
    expect(() => validateVideoOptions({ container, src: '/a.mp4' })).not.toThrow()
  })

  it('defaults to muted', () => {
    // Every modern browser blocks unmuted autoplay. The legacy default was
    // unmuted, so an autoplay video simply never started.
    expect(validateVideoOptions({ container, src: '/a.mp4' }).muted).toBe(true)
  })

  it('honours an explicit muted: false', () => {
    expect(validateVideoOptions({ container, src: '/a.mp4', muted: false }).muted).toBe(false)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npm run test:unit -- constructor-validation`
Expected: FAIL —— 无法解析 `../../src/viewer/options`

- [ ] **Step 3: 实现 `options.ts`**

`src/viewer/options.ts`：

```ts
/**
 * Constructor argument validation.
 *
 * There is far less of this than the legacy code had. `param-check` guarded
 * every public boundary against every possible wrong type; in TypeScript the
 * type system already does that, and a runtime check for "is this a string"
 * adds nothing for anyone using the library from TypeScript.
 *
 * What survives is the class of mistake the type system cannot see:
 *
 * - A value that is the right type but the wrong range or the wrong member of
 *   a union, arriving through a cast or from JavaScript.
 * - Options that USED to exist and are now gone. Ignoring one silently leaves
 *   an upgrader believing it still works.
 */

import type { TextureProjection } from '../core/constants'
import type { CameraOptions } from './types'

/**
 * The texture projections this viewer accepts.
 *
 * An alias of `TextureProjection`, not a union of its own and not a copy of its
 * members: there is exactly one accepting type, and `'fisheye'` is deliberately
 * NOT in it. Fisheye was documented in the legacy library and never implemented
 * -- the shader returned `vec2(0.0)`. Leaving it in the type would let a
 * TypeScript caller write an option that can only ever throw; the runtime check
 * below still recognises the string, because a JavaScript caller or a legacy
 * snippet has no compiler to stop it, and "not implemented" is a better message
 * than "unknown projection".
 *
 * The day fisheye is implemented, this is the one line that widens.
 */
export type ImageProjection = TextureProjection

export interface ImageViewerOptions {
  readonly container: HTMLElement
  readonly src: string
  /** How the source's pixels are laid out. A property of the source, not the camera. */
  readonly projection?: ImageProjection
  readonly camera?: CameraOptions
  readonly PTZ?: boolean
}

export interface VideoViewerOptions extends ImageViewerOptions {
  readonly autoplay?: boolean
  readonly loop?: boolean
  readonly muted?: boolean
}

/** Options that were removed. Present only so they can be rejected loudly. */
interface RemovedOptions {
  readonly frameSize?: unknown
  readonly el?: unknown
}

function assertContainer (value: unknown): asserts value is HTMLElement {
  if (value === null || typeof value !== 'object' || (value as Node).nodeType !== 1) {
    throw new TypeError('container must be an HTMLElement')
  }
}

/**
 * Asserts a source URL.
 *
 * Exported because the `src` setters on both public classes take a URL long
 * after construction, and they must reject the same values the constructor does
 * -- a setter that accepted `''` would be the constructor's bug, one call later.
 */
export function assertSrc (value: unknown): asserts value is string {
  if (typeof value !== 'string') throw new TypeError('src must be a string')
  // An empty src makes the browser resolve it to the current page URL, load the
  // HTML document as an image, fail to decode it, and report a media-error that
  // says nothing about the real cause.
  if (value.trim() === '') throw new TypeError('src must not be empty')
}

/**
 * Asserts a texture projection and narrows it for the caller.
 *
 * An assertion function rather than a plain check so that after it returns,
 * `options.projection` is `ImageProjection | undefined` and can be handed to
 * `ImageSource` / `VideoSource` without a cast. `undefined` passes: it means
 * "equirectangular", which is what those constructors default to.
 */
function assertProjection (value: unknown): asserts value is ImageProjection | undefined {
  if (value === undefined) return
  if (value === 'equiprectangular') {
    throw new TypeError(
      "projection 'equiprectangular' was a misspelling; use 'equirectangular'"
    )
  }
  if (value === 'fisheye') {
    // Rejecting at construction. The legacy code accepted it here and threw
    // from inside the texture update, so the viewer reported success and then
    // failed on the first frame.
    throw new TypeError("projection 'fisheye' is not implemented")
  }
  if (value !== 'equirectangular') {
    throw new TypeError(`unknown projection: ${String(value)}`)
  }
}

function assertNoRemoved (options: RemovedOptions): void {
  if (options.frameSize !== undefined) {
    throw new TypeError(
      'the frameSize option was removed: WebGPU has no power-of-two texture ' +
      'requirement, and oversize sources are downscaled automatically'
    )
  }
  if (options.el !== undefined) {
    throw new TypeError("the el option was renamed to 'container'")
  }
}

/**
 * Validates image viewer options.
 *
 * The parameter is the intersection with `RemovedOptions` so that a removed
 * option is *reachable* at runtime -- a caller from JavaScript, or a TypeScript
 * caller who has not rebuilt yet, still arrives here, and the point of checking
 * is to tell them. A literal with `frameSize` in TypeScript is additionally
 * refused by the excess-property check at the call site, which is the better
 * error of the two.
 */
export function validateImageOptions (options: ImageViewerOptions & RemovedOptions): ImageViewerOptions {
  assertContainer(options.container)
  assertSrc(options.src)
  assertProjection(options.projection)
  assertNoRemoved(options)
  return options
}

/** Validates video viewer options, filling in the autoplay defaults. */
export function validateVideoOptions (
  options: VideoViewerOptions & RemovedOptions
): VideoViewerOptions & { muted: boolean } {
  validateImageOptions(options)
  return {
    ...options,
    // Muted by default. Every modern browser blocks unmuted autoplay, so an
    // autoplay video with sound never starts -- which is what the legacy
    // default produced, with no error.
    muted: options.muted ?? true
  }
}
```

- [ ] **Step 4: 实现两个公开类与 `index.ts`**

`src/viewer/image-viewer.ts`：

```ts
/**
 * A full-screen equirectangular image viewer.
 *
 * @example
 * ```ts
 * const viewer = await FramelessImageViewer.create({
 *   container: document.querySelector('#pano')!,
 *   src: '/panorama.jpg'
 * })
 * viewer.on('media-load', () => console.log('ready'))
 * ```
 */

import { Viewer } from './viewer'
import { validateImageOptions, type ImageViewerOptions } from './options'
import { createBackend, probe } from './backend-factory'
import { ImageSource } from '../media/image-source'
import type { Capabilities } from '../renderer/backend'

export class FramelessImageViewer extends Viewer {
  readonly #source: ImageSource

  private constructor (options: ImageViewerOptions, source: ImageSource, deps: ConstructorParameters<typeof Viewer>[0]) {
    super(deps)
    this.#source = source
    this.source = source
    // Re-emit every media event on the viewer, so consumers listen in one place.
    for (const type of ['media-load', 'media-error', 'media-play', 'media-pause', 'media-ended', 'media-seeking', 'media-seeked', 'media-progress'] as const) {
      source.on(type, (event) => this.events.emit(type, event))
    }
    if (options.PTZ === false) this.PTZ = false
  }

  /**
   * Creates a viewer, or throws.
   *
   * Async because acquiring a GPU device is. Throws rather than returning a
   * viewer that cannot draw: the legacy `createProgram` logged and returned
   * null, and the viewer reported success and rendered nothing forever.
   *
   * @throws If no backend is available, if the shader fails to compile, or if
   *   the options are invalid.
   */
  static async create (options: ImageViewerOptions): Promise<FramelessImageViewer> {
    const valid = validateImageOptions(options)
    const container = document.createElement('div')
    valid.container.appendChild(container)

    const backend = await createBackend(container.ownerDocument.createElement('canvas'))
    const source = new ImageSource(valid.src, {
      maxTextureDimension: backend.capabilities.maxTextureDimension
    })

    return new FramelessImageViewer(valid, source, {
      container,
      camera: valid.camera,
      backend
    })
  }

  get src (): string { return this.#source.naturalSize.width > 0 ? this.#sourceUrl : '' }
  #sourceUrl = ''

  /** The rendering capabilities actually in use. */
  get capabilities (): Capabilities { return this.super.capabilities }

  /** Probes the device without constructing a viewer. */
  static probe = probe
}
```

> **上面的 `#sourceUrl` / `capabilities` 是待补齐的接线。** `create()` 拿到了 `backend`，但 `Viewer` 需要暴露 `capabilities`。**执行者把 `Viewer` 的 `#backend` 改成 `protected readonly backend`，然后在 `Viewer` 上加 `get capabilities(): Capabilities { return this.backend.capabilities }`** —— 不要在两个子类里各存一份。
>
> `src` 的读写：`Viewer` 持有 `protected source`，但换源需要销毁旧的并接上新的。**把它做成 `Viewer` 上的方法 `protected setSource(source: MediaSource): void`（负责 dispose 旧的、重挂事件、重置 `#lastSourceVersion`），两个子类只暴露 getter/setter。** 不要让子类自己实现换源逻辑。

`src/viewer/video-viewer.ts` 与 image 版同构，差异只有：用 `VideoSource`、传 `autoplay`/`loop`/`muted`、多一个 `play()`/`pause()` 转发。

`src/index.ts`：

```ts
/**
 * pano.gl -- a WebGPU viewer for equirectangular 360 images and video.
 *
 * The exported surface is deliberately small and is FROZEN as of v1.0.
 * Everything else in `src/` is internal and may be reorganised.
 */

export { FramelessImageViewer } from './viewer/image-viewer'
export { FramelessVideoViewer } from './viewer/video-viewer'
export type { ImageViewerOptions, VideoViewerOptions, ImageProjection } from './viewer/options'
export type { CameraOptions } from './viewer/types'
export type { Capabilities } from './renderer/backend'
export { CameraProjection } from './core/constants'
```

- [ ] **Step 5: 跑测试**

Run: `npm run test:unit -- constructor-validation && npm run typecheck`
Expected: 11 个测试 PASS，类型检查通过

- [ ] **Step 6: Commit**

```bash
git add src/viewer/options.ts src/viewer/image-viewer.ts src/viewer/video-viewer.ts src/index.ts test/unit/constructor-validation.test.ts
git commit -m "feat(viewer): public classes and a frozen export surface

Runtime validation is only for what the type system cannot see: the
misspelled 'equiprectangular', the fisheye projection that used to fail on
the first frame instead of at construction, and options that were removed."
```

---

### Task 5: 用户故事级集成测试

**Files:**
- Test: `test/integration/user-story-photo.test.ts`
- Test: `test/integration/user-story-video.test.ts`
- Test: `test/integration/user-story-camera-switch.test.ts`
- Test: `test/integration/user-story-media-failure.test.ts`
- Test: `test/integration/user-story-no-webgpu.test.ts`

**每个 User Story 一个文件**，覆盖全部功能点与异常场景。

- [ ] **Step 1: US1 —— 看一张全景照片并拖动**

```ts
import { test, expect } from './support/fixtures'

test.describe('US1: view a 360 photo and look around', () => {
  test('renders the image and reports load', async ({ gpuPage }) => {
    const r = await gpuPage.evaluate(async () => {
      const t = window.__panoTest
      const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
      const loaded = await t.waitFor(viewer, 'media-load', 5000)
      const pixels = await t.readCanvas(viewer)
      viewer.dispose()
      return { loaded, nonBlack: t.countNonBlack(pixels) }
    })
    expect(r.loaded).toBe(true)
    expect(r.nonBlack).toBeGreaterThan(0.5)
  })

  test('dragging changes the rendered image', async ({ gpuPage }) => {
    const r = await gpuPage.evaluate(async () => {
      const t = window.__panoTest
      const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
      await t.waitFor(viewer, 'media-load', 5000)
      const before = await t.readCanvas(viewer)
      t.drag(viewer, 200, 0)
      const after = await t.readCanvas(viewer)
      const rotations: unknown[] = []
      viewer.on('rotate', e => rotations.push(e))
      viewer.dispose()
      return { diff: t.maxDiff(before, after), rotations: rotations.length }
    })
    expect(r.diff).toBeGreaterThan(2)
  })

  test('PTZ = false leaves the image static', async ({ gpuPage }) => {
    const r = await gpuPage.evaluate(async () => {
      const t = window.__panoTest
      const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png', PTZ: false })
      await t.waitFor(viewer, 'media-load', 5000)
      const before = await t.readCanvas(viewer)
      t.drag(viewer, 200, 200)
      const after = await t.readCanvas(viewer)
      viewer.dispose()
      return t.maxDiff(before, after)
    })
    expect(r).toBeLessThanOrEqual(2)
  })

  test('the image fills a container that changes size', async ({ gpuPage }) => {
    // The legacy code listened on window resize and additionally called
    // window.removeEventLstener (a typo), so the listener never came off. A
    // ResizeObserver also catches container-only changes, which is the common
    // case for a viewer inside a layout.
    const r = await gpuPage.evaluate(async () => {
      const t = window.__panoTest
      const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
      await t.waitFor(viewer, 'media-load', 5000)
      const before = t.canvasSize(viewer)
      t.container(viewer).style.width = '300px'
      await t.waitForFrames(3)
      const after = t.canvasSize(viewer)
      viewer.dispose()
      return { before, after }
    })
    expect(r.after.width).not.toBe(r.before.width)
  })

  test('renders sharply on a high-DPI display', async ({ gpuPage }) => {
    // The legacy Renderer.adjustSize ignored devicePixelRatio entirely, so every
    // retina display got a blurry upscale.
    const r = await gpuPage.evaluate(async () => {
      const t = window.__panoTest
      const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png', dpr: 2 })
      await t.waitFor(viewer, 'media-load', 5000)
      const size = t.canvasSize(viewer)
      const css = t.container(viewer).getBoundingClientRect()
      viewer.dispose()
      return { backing: size, css: { width: css.width, height: css.height } }
    })
    expect(r.backing.width).toBeCloseTo(r.css.width * 2, -1)
  })
})
```

- [ ] **Step 2: US2 —— 播放全景视频并缩放**

```ts
test.describe('US2: play a 360 video and zoom', () => {
  test('plays and advances frames', async ({ gpuPage }) => { /* play, sample canvas twice, assert diff > 2 */ })
  test('a source swap tears down the old element', async ({ gpuPage }) => { /* set src, assert previous video paused + listeners 0 */ })
  test('autoplay is muted by default', async ({ gpuPage }) => { /* assert element.muted === true */ })
  test('zoom changes the rendering for a non-linear camera', async ({ gpuPage }) => { /* cylindrical + wheel */ })
  test('zoom is a no-op for the linear camera', async ({ gpuPage }) => { /* perspective + wheel, assert no diff */ })
  test('play() on a disposed viewer throws', async ({ gpuPage }) => { /* */ })
})
```

- [ ] **Step 3: US3 —— 运行时切换相机模型**

```ts
test.describe('US3: switch camera models at runtime', () => {
  for (const kind of ['linear', 'cylindrical', 'planet', 'pannini'] as const) {
    test(`switches to ${kind} and keeps the pose`, async ({ gpuPage }) => {
      // The legacy cameraOptions setter rebuilt the whole camera and reset the
      // pose to the origin. Changing the projection silently threw away where
      // the user was looking.
      const r = await gpuPage.evaluate(async k => {
        const t = window.__panoTest
        const viewer = await t.createImageViewer({ src: '/fixtures/panorama.png' })
        await t.waitFor(viewer, 'media-load', 5000)
        viewer.rotate(20, 100)
        const poseBefore = viewer.cameraOptions.pose
        const pixelsBefore = await t.readCanvas(viewer)
        viewer.cameraOptions = { ...viewer.cameraOptions, projection: { kind: k, zoom: 1, extent: [1, 1] } }
        await t.waitForFrames(3)
        const poseAfter = viewer.cameraOptions.pose
        const changed = t.maxDiff(await t.readCanvas(viewer), pixelsBefore)
        viewer.dispose()
        return { before: poseBefore, after: poseAfter, changed }
      }, kind)
      expect(r.after).toEqual(r.before)
      expect(r.changed).toBeGreaterThan(2)
    })
  }
})
```

- [ ] **Step 4: US4 —— 素材加载失败**

```ts
test.describe('US4: media fails to load', () => {
  test('a 404 image emits media-error and does not throw', async ({ gpuPage }) => { /* */ })
  test('an undecodable image emits media-error', async ({ gpuPage }) => { /* serve a text file at a .png URL */ })
  test('a video with a bad codec emits media-error', async ({ gpuPage }) => { /* */ })
  test('the viewer keeps rendering the background after a source error', async ({ gpuPage }) => { /* */ })
  test('constructing with an empty src throws at construction', async ({ gpuPage }) => { /* */ })
})
```

- [ ] **Step 5: US5 —— 没有 WebGPU 的环境**

```ts
test.describe('US5: running where WebGPU is unavailable', () => {
  test('create() throws a message naming the backend situation', async ({ gpuPage }) => {
    // Runs in the default headless shell, where navigator.gpu exists but
    // requestAdapter() returns null. Until P6 lands the WebGL2 backend, this is
    // the honest outcome: a clear failure, not a black rectangle.
    const message = await gpuPage.evaluate(async () => {
      const t = window.__panoTest
      try {
        await t.createImageViewer({ src: '/fixtures/panorama.png' })
        return ''
      } catch (e) { return String(e) }
    })
    expect(message).toMatch(/no usable rendering backend/i)
  })

  test('probe() reports the fallback rather than throwing', async ({ gpuPage }) => {
    // probe() must be usable to decide what to do BEFORE constructing anything.
    const caps = await gpuPage.evaluate(async () => {
      const t = window.__panoTest
      return t.probe()
    })
    expect(['webgpu', 'webgl2', 'none']).toContain(caps.backend)
  })
})
```

- [ ] **Step 6: 跑全部**

Run: `npm run test:unit && npm run test:integration`
Expected: 全 PASS

- [ ] **Step 7: Commit**

```bash
git add test/integration/
git commit -m "test(viewer): user-story integration suite

One file per user story. Covers the happy path, the gesture paths, runtime
camera switching, media failure, and the no-WebGPU environment."
```

---

## 完成标准

- [ ] 五个 User Story 全绿，包含各自的异常场景
- [ ] `npm run test:unit` 覆盖率过关，`npm run typecheck` 干净
- [ ] 静止图片在 1 秒内产生的 draw 次数 < 10
- [ ] `dispose()` 后：draw 数不再增长、监听器数归零、公开方法抛异常
- [ ] `create()` 在无后端时抛异常，且消息说清是哪个后端不可用
- [ ] **公开 API 冻结** —— `src/index.ts` 的导出面自此不再变（P6/P7 不改它）

## 交给下游的东西

| 产物 | 消费者 |
|---|---|
| `src/index.ts` 的导出面 | P6 必须保持它不变 —— WebGL2 后端是内部替换 |
| `createBackend()` | P6 在这里加 WebGL2 分支 |
| `Viewer.#drawFrame` 的调用序列 | P6 的 `WebGL2Backend` 必须满足同样的 `setSource`/`setCamera`/`render` 契约 |
| 五个 User Story 测试 | P6 让它们**在 WebGL2 后端下也全部跑一遍** —— 这是后端替换的验收方式 |
