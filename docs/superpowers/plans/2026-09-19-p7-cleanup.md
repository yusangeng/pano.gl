# P7 — 删旧代码与收尾 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development（无 subagent 则 executing-plans）；收尾走 superloop 的 task-finish，禁用 finishing-a-development-branch。

**Goal:** 旧代码消失，依赖降到 3 个，文档与新 API 对齐。

**Architecture:** 纯删除与文档。**这一期不写任何新功能** —— 如果这一期需要写新代码，说明前面某一期漏了。

**Tech Stack:** 无新增

---

## 这一期的性质

**这是唯一一期「删得越多越成功」的工作。** 前面六期都克制着没删旧代码，因为它一直是参考资料（门禁 A 拿它当基准，P6 拿它对拍）。

**P7 之后，回不去了。** 所以有几件事必须在动手前确认：

- [ ] P3 的门禁 A、B 已通过并留下记录
- [ ] P6 的门禁 C 已通过并留下记录
- [ ] P0 的基线 fixture 已提交（删除旧代码不影响它，但它是唯一的对拍依据了）
- [ ] v1.0.0 已经从主分支发过（或至少 `npm run build` 的产物已验证可用）

**任何一条没勾，停下。** 先回去补，不要靠「反正 git 里有历史」自我安慰 —— git 里有历史是真的，但**让历史重新跑起来**（Babel 6 + webpack 3 + Node 9）在 2026 年是个独立项目。

---

## File Structure

| 路径 | 处置 |
|---|---|
| `legacy/` | **删除**（P1 迁进去的 v0.2.2 全部源码） |
| `webpack/` | **删除**（debug.js / release.js） |
| `.babelrc` | **删除** |
| `vendor/cuon.js` | **删除** |
| `scripts/legacy-build.mjs` | **删除** |
| `tsconfig.legacy.json` | **删除** |
| `.travis.yml` | **删除** → 换成 `.github/workflows/ci.yml` |
| `demo/Index.js`, `demo/webpack.config.js`, `demo/index.html`, `demo/index.css` | **删除**（旧的；P1 已建了新的） |
| `demo/main.ts` | 保留，这一期补完 |
| `demo/image/`, `demo/video/` | **保留**（演示素材，与实现无关） |
| `demo/libs/`, `demo/fonts/` | 待查（见 Task 1） |
| `package.json` | 重写 `dependencies` / `devDependencies` / `scripts` |
| `README.md` | 重写 |
| `docs/migration-v0.2-to-v1.0.md` | **新建** |
| `docs/api/` | typedoc 输出（**不是 `docs/`**，见 Task 4） |

---

### Task 1: 删之前先查清依赖

**Files:**
- Create: `docs/superpowers/notes/p7-deletion-audit.md`

**为什么先查**：`legacy/` 和 `demo/` 里有 33MB 素材和一堆看不清用途的文件（`demo/libs`、`demo/fonts`）。**盲删会让 demo 挂掉，而且要到下次有人打开 demo 时才发现。**

- [ ] **Step 1: 查每个待删文件有没有活引用**

```bash
cd /Users/yusangeng/workspace/pano.gl

# Everything outside legacy/ that still mentions legacy/
git grep -n "legacy/" -- . ':(exclude)legacy' ':(exclude)docs' ':(exclude)package-lock.json'

# Everything that mentions the vendored matrix library
git grep -n "cuon" -- . ':(exclude)vendor' ':(exclude)docs'

# What the old demo pulls in
cat demo/Index.js
cat demo/webpack.config.js

# Sizes, so the audit records what was actually removed
du -sh demo/* vendor legacy 2>/dev/null | sort -h
```

- [ ] **Step 2: 判定 demo 素材的去留**

对 `demo/libs/` 和 `demo/fonts/` 逐个判定，**把结论写进 audit**：

| 文件 | 判定问题 |
|---|---|
| `demo/libs/**` | 是旧 demo 的运行时依赖（如某个 UI 库）还是纯素材？旧 demo 删掉后谁还用？ |
| `demo/fonts/**` | 是 demo 页面用的字体吗？新 `demo/index.html`（P1 建的）引用了没有？ |

**判定规则：新 demo 用得到就留，用不到就删。** 「也许以后用得上」不算理由 —— git 里有。

`demo/image/` 与 `demo/video/` **保留**：它们是演示素材，新 demo 要展示全景图和全景视频，这是 demo 存在的原因。**但要在 audit 里记下它们的体积** —— 33MB 的素材放在一个 npm 包里是另一回事（`.npmignore` 已经排除 `demo`，所以不进包，没问题）。

- [ ] **Step 3: 写 audit**

`docs/superpowers/notes/p7-deletion-audit.md`：

```markdown
# P7 deletion audit

Recorded before deleting anything, so that "why is this gone" has an answer
that is not `git log --follow`.

## Removed

| Path | Reason | Verified no live reference |
|---|---|---|
| `legacy/` | v0.2.2 sources, superseded |  |

## Kept

| Path | Reason |
|---|---|
|  |  |

## Removed assets

| Path | Size | Reason |
|---|---|---|
|  |  |  |
```

**填满它，不要留空行。** 这份文档的价值就在「每一条都有依据」。

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/notes/p7-deletion-audit.md
git commit -m "docs: audit what P7 removes and why

Recorded before the deletion so the answer to 'why is this gone' is not
'git log --follow'."
```

---

### Task 2: 删除旧代码

**Files:**
- Delete: `legacy/`, `webpack/`, `.babelrc`, `vendor/`, `scripts/legacy-build.mjs`, `tsconfig.legacy.json`, `.travis.yml`
- Delete: `demo/Index.js`, `demo/webpack.config.js`, `demo/index.html`, `demo/index.css`
- Delete: whatever Task 1's audit marked as removed assets

- [ ] **Step 1: 先确认现在是绿的**

```bash
npm run typecheck && npm run test:unit && npm run build
```

Expected: 全绿。**红着删代码，之后分不清哪个红是自己弄出来的。**

- [ ] **Step 2: 删**

```bash
git rm -r legacy webpack vendor
git rm .babelrc tsconfig.legacy.json .travis.yml
git rm scripts/legacy-build.mjs
git rm demo/Index.js demo/webpack.config.js demo/index.html demo/index.css
# 加上 Task 1 audit 里判定为删除的素材
```

> **`demo/index.html` 与 `demo/index.css` 只删旧的。** P1 的 Task 8 建了新的 `demo/index.html` 和 `demo/main.ts` —— 如果 P1 当时是就地改的而不是新建，**这里不要删它们**，先 `cat` 一眼确认内容是新实现（import 自 `../src`）还是旧的（`<script src="../.package/bundle.js">`）。

- [ ] **Step 3: 清掉所有悬空引用**

```bash
# 不应再有任何命中
git grep -n "legacy/" -- . ':(exclude)docs' ':(exclude)package-lock.json'
git grep -n "cuon"
git grep -n "babel" -- . ':(exclude)package-lock.json' ':(exclude)docs'
git grep -n "webpack" -- . ':(exclude)package-lock.json' ':(exclude)docs'
```

Expected: 每条 `(empty)`。

**`docs/` 被排除是因为 spec 和 plan 里合法地提到这些词**（它们描述的就是这次迁移）。**除了 docs，一处都不该剩。**

- [ ] **Step 4: 重建并验证**

```bash
rm -rf node_modules dist package-lock.json
npm install
npm run build && npm run test:unit && npm run typecheck
```

Expected: 全绿。

**`rm -rf package-lock.json` 是必须的** —— 旧 lock 里锁着几百个 Babel/webpack 时代的包，不清掉 `npm install` 会把它们留在树里，Task 3 的依赖数检查就假绿了。

- [ ] **Step 5: 跑集成测试**

```bash
npx playwright install --with-deps chromium
npm run test:integration
```

Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: remove the v0.2.2 implementation

Legacy sources, the Babel 6 / webpack 3 toolchain, the vendored matrix
library and the old demo. The reference implementation has served its
purpose: gates A, B and C are recorded against it, and the v1.0.0
behaviour no longer depends on being able to run it."
```

---

### Task 3: 依赖降到 3 个

**Files:**
- Modify: `package.json`
- Modify: `.npmignore`

- [ ] **Step 1: 重写 `package.json`**

```json
{
  "name": "pano.gl",
  "version": "1.0.0",
  "description": "WebGPU viewer for equirectangular (360°) images and video, with a WebGL2 fallback.",
  "type": "module",
  "main": "./dist/pano-gl.js",
  "module": "./dist/pano-gl.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/pano-gl.mjs",
      "require": "./dist/pano-gl.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "dev": "vite",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run && playwright test",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:integration": "playwright test",
    "doc": "typedoc"
  },
  "dependencies": {
    "debug": "^4.4.3",
    "gl-matrix": "^3.4.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/debug": "^4.1.12",
    "@vitest/coverage-v8": "^2.1.0",
    "tsup": "^8.3.0",
    "typedoc": "^0.27.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
```

**三个决定：**

| 决定 | 理由 |
|---|---|
| `sideEffects: false` | 全库没有模块级副作用（P1 的 `src/index.ts` 只做 re-export）。**这一行是打包器做 tree-shaking 的前提**，少了它消费者会把整个库拖进去。 |
| `exports` 映射含 `types` | `types` 必须是每个条件分支的**第一条**。放错顺序 TypeScript 会拿到 JS 文件当声明文件，报「找不到类型」。 |
| `engines: node >= 20` | Node 18 已 EOL，而 `crypto.randomUUID()` 在全局可用是 Node 19+。 |

> **`version` 到 1.0.0** —— 这是 P5 冻结的公开 API，P6 只加了一个后端，没有改接口。

- [ ] **Step 2: 验证依赖数**

```bash
node -e "
const p = require('./package.json');
const runtime = Object.keys(p.dependencies).sort();
console.log('runtime deps:', runtime.length, runtime.join(', '));
" 
```

Expected: `runtime deps: 2 debug, gl-matrix`

**说「3 个」的口径**：`debug` 带一个运行时依赖 `ms`（spec §7.3 实测），所以**装出来的运行时树是 3 个包**：`gl-matrix`、`debug`、`ms`。上面这个脚本数的是**直接声明**（2 个）。

两个数都要对，别混淆：

```bash
npm ls --omit=dev --all | tail -5
```

Expected: 树里恰好 3 个包。

- [ ] **Step 3: 确认没有残留**

```bash
npm ls --omit=dev --all 2>&1 | grep -Ei "babel|webpack|lodash|chivy|dodele|litchy|konph|polygala|shortid|param-check|isparta|istanbul|mocha|chai" || echo "clean"
```

Expected: `clean`

**`chai` 与 `mocha` 是 devDependencies**，但也要确认它们走了 —— 测试框架换成了 vitest，留着 chai 只会让人以为还能用。

- [ ] **Step 4: 在干净的树上装一遍**

```bash
rm -rf node_modules package-lock.json
npm install
npm run build
ls -la dist/
```

Expected: `dist/` 里有 `pano-gl.js`、`pano-gl.mjs`、`index.d.ts`。

**再确认产物是自包含的** —— 这是 §8 要解决的旧痛点：

```bash
grep -c "require(" dist/pano-gl.js
grep -o "textureSampleBaseClampToEdge\|@fragment" dist/pano-gl.js | head -3
```

Expected: 第二条件命中 —— **WGSL 源码已经被内联进产物**。如果没命中，说明着色器还在作为外部文件被 `import`，消费者就得自带 loader，正是旧设计的问题。

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json .npmignore
git commit -m "chore: 8 runtime dependencies down to 3 packages

gl-matrix, debug and debug's own dependency ms. Everything else -- the
litchy/dodele/param-check/chivy cluster, lodash, shortid, konph, polygala
and the vendored cuon -- is gone rather than replaced."
```

---

### Task 4: 文档

**Files:**
- Modify: `README.md`
- Create: `docs/migration-v0.2-to-v1.0.md`
- Modify: `tsconfig.json`（typedoc 输出目录）

**为什么 typedoc 不能输出到 `docs/`**：spec §8 写的是 `docs/`，但 `docs/` 已经是 `docs/superpowers/` 的家。typedoc 会往输出目录里写 `index.html`、`assets/`、`modules/` —— 和 spec 混在一个目录里，`npm run doc` 一次就把文档树搅乱了。**改成 `docs/api/`。**

- [ ] **Step 1: 改 typedoc 输出**

`package.json` 里：

```json
"doc": "typedoc --out docs/api src/index.ts"
```

并把 `docs/api/` 加进 `.gitignore`（生成物不入库）。

- [ ] **Step 2: 写迁移指南**

`docs/migration-v0.2-to-v1.0.md`：

```markdown
# Migrating from pano.gl 0.2.x to 1.0.0

1.0.0 is a rewrite. The public surface is smaller and the constructor options
changed. There is no compatibility shim: the shapes were close enough that a
shim would have preserved the parts worth changing.

## Constructor options

| 0.2.x | 1.0.0 | Note |
|---|---|---|
| `el` | `container` | The same element, under the name the rest of the ecosystem uses. |
| `src` | `src` | Unchanged. |
| `projection: 'equiprectangular'` | `projection: 'equirectangular'` | **The old value was misspelled.** The correct spelling is now the only accepted one. |
| `projection: 'fisheye'` | —— | Throws at construction. It never worked: the legacy shader sampled `vec2(0.0, 0.0)` for every fisheye pixel, and the JS path threw. |
| `frameSize` | —— | Deleted. It existed to work around WebGL1's power-of-two texture rule, which neither WebGPU nor WebGL2 has. |
| `camera: { type, ... }` | `camera: { kind, ... }` | See below. |

## Camera types

| 0.2.x | 1.0.0 |
|---|---|
| `'perspective'` | `{ kind: 'linear' }` |
| `'cylindrical'` | `{ kind: 'cylindrical' }` |
| `'planet'` | `{ kind: 'planet' }` |
| `'pannini'` | `{ kind: 'pannini' }` |

`'ortho'` was never a public option; it is gone entirely.

## Behaviour changes

**Non-linear cameras now respond to latitude.** In 0.2.x, setting
`povLatitude` on the cylindrical, planet and pannini cameras had no effect:
the uniform was uploaded and the shader never read it. It does now. If your
application was compensating for the old behaviour by rotating the image
itself, remove that compensation.

**This is the only intentional behaviour change in 1.0.0.** Everything else
renders to the same pixels, which is what the migration's three pixel gates
were for.

## 事件

`on('*', handler)` 不再存在 —— 它转发一切，包括你不想接的。改为按名字订阅；`on()` 返回你自己的取消函数，不再需要记住 `off` 的签名。

## 渲染后端

1.0.0 优先 WebGPU，没有则降到 WebGL2。检查 `viewer.capabilities.backend` 可知道用了哪个。**两者渲染同样的像素**（门禁 C 断言 ±2 LSB）。

## 清理

`dispose()` 仍然是必须的，而且现在真的会释放资源：WebGL2 走 `WEBGL_lose_context`（0.2.x 从不释放上下文，创建 16 个 viewer 后新的就静默失败），DOM 监听走 `AbortController`（0.2.x 的 `removeEventLstener` 拼错了，resize 监听从来没被移除过）。
```

> **文档用中文还是英文？** 迁移指南面向**现有使用者** —— 那是一个人（作者本人）加上可能不知道有多少的下游。**先用中文写。** README 面向 npm 上的陌生人，用英文。

- [ ] **Step 3: 重写 README**

README 的结构（**内容由执行者写，这里只定骨架和必须有的东西**）：

1. **一句话是什么** —— WebGPU 全景查看器，WebGL2 降级
2. **浏览器支持** —— 表格，明确 WebGPU 与 WebGL2 的覆盖
3. **安装与最小示例** —— 十行以内能跑起来
4. **构造选项表** —— 与迁移指南同源
5. **事件表**
6. **`capabilities` 说明**
7. **四个投影的对照图**（用 `demo/image/` 里的素材截图 —— **这一项是真的有用，不是装饰**：四个投影的区别光看名字看不出来）
8. **开发** —— `npm run dev` / `test` / `build`
9. **迁移** —— 链接到 `docs/migration-v0.2-to-v1.0.md`

**删掉旧 README 里的一切**：standardjs 徽章（项目没在用）、travis 徽章（要换成新的 CI）、`BundleSizeDebug.html` 的说明（那个产物没了）。

- [ ] **Step 4: 验证示例能跑**

把 README 里的最小示例**逐字**拷进 `demo/main.ts` 跑一遍：

```bash
npm run dev
```

Expected: 浏览器打开，全景图正常渲染，拖拽和滚轮有效。

**这一步不能省。** README 里的示例代码烂掉是最常见的文档失败，而它一旦烂了，第一个新用户就会撞上。

- [ ] **Step 5: Commit**

```bash
git add README.md docs/migration-v0.2-to-v1.0.md package.json .gitignore
git commit -m "docs: README and the 0.2.x -> 1.0.0 migration guide

typedoc now writes to docs/api/ rather than docs/: docs/ is where the
superpowers specs live, and generating into it would mix the two."
```

---

### Task 5: CI

**Files:**
- Create: `.github/workflows/ci.yml`
- Delete: `.travis.yml`（Task 2 已删）

**`.travis.yml` 指向 Node 9**，那已经不存在了。而且它只跑 `npm run build` —— **没有测试**。这正是「项目没有测试」这件事能持续八年的原因之一。

- [ ] **Step 1: 写 workflow**

`.github/workflows/ci.yml`：

```yaml
name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npm run typecheck

      - run: npm run test:coverage

      # WebGPU is not available in the default headless browser, so the
      # integration suite drives Chromium with the flags that turn it on.
      # --enable-unsafe-webgpu is required: without it navigator.gpu exists but
      # requestAdapter() resolves to null, and every test would take the WebGL2
      # path while reporting success.
      - run: npx playwright install --with-deps chromium

      - run: npm run test:integration
        env:
          PLAYWRIGHT_CHROMIUM_ARGS: '--enable-unsafe-webgpu --enable-features=Vulkan'

      - run: npm run build

      # A WebGPU library that silently ships without WebGPU support is the
      # failure this step exists to catch. The assertion is in the test suite,
      # but a green suite that never reached the WebGPU path looks identical
      # to a green suite that did.
      - name: assert the WebGPU path actually ran
        run: |
          if grep -rq "skipped: no webgpu adapter" test-results/ 2>/dev/null; then
            echo "integration tests fell back to WebGL2; WebGPU was not exercised"
            exit 1
          fi
```

- [ ] **Step 2: 本地模拟一遍**

```bash
npm run typecheck && npm run test:coverage && npm run test:integration && npm run build
```

Expected: 全绿。

**覆盖率门槛要真的在卡**，不能只是打印：

```bash
node -e "
const c = require('./coverage/coverage-summary.json').total;
const branch = c.branches.pct;
console.log('branch coverage:', branch + '%');
process.exit(branch >= 90 ? 0 : 1);
"
```

Expected: 退出码 0，`branch coverage: 9x%`

**把这一段加进 `test:coverage` 脚本**（`vitest run --coverage && node scripts/check-coverage.mjs`）。**只在 CI 里检查覆盖率是不够的** —— 本地能跑绿、推上去才发现分支覆盖不到 90%，是白跑一轮。

- [ ] **Step 3: 推一个空提交验证 CI 真的会跑**

```bash
git commit --allow-empty -m "ci: trigger"
git push
```

Expected: GitHub Actions 起来，全绿。

**然后删掉那个空提交**（`git reset --hard HEAD~1 && git push --force-with-lease`）—— 或者留着，无所谓，重要的是**确认过一次 CI 真的在跑**。

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/ci.yml scripts/check-coverage.mjs package.json
git commit -m "ci: replace the travis config, and actually run the tests

.travis.yml targeted Node 9 and ran only 'npm run build' -- no tests at
all, which is part of how this project went eight years without a suite."
```

---

### Task 6: 收尾检查

**Files:**
- 无新增

- [ ] **Step 1: 死代码**

```bash
npx tsc --noEmit --noUnusedLocals --noUnusedParameters
npx knip --include files,exports,types,dependencies
```

Expected: 无输出。

**`knip` 是一次性的检查，不进 CI**（它误报多，进了 CI 会被加一堆 ignore，然后就没人看了）。**跑一次，处理掉真问题，然后把 `knip` 本身卸掉。**

**它会报的一类真问题**：`src/index.ts` 导出了某个类型但没有任何测试或文档引用它。要么删，要么加进 README —— **「公开 API 冻结」意味着导出的每一样东西都有人依赖，多导出的类型也是承诺。**

- [ ] **Step 2: 依赖方向**

分层规则（`foundation` < `domain` < `app` < `ui`）不能有反向依赖：

```bash
# core/ 不许 import 别的层
git grep -n "from '\.\./\(media\|interaction\|viewer\|renderer\)/" -- src/core/ || echo "clean: core"
# media/ 不许 import 上层
git grep -n "from '\.\./\(interaction\|viewer\)/" -- src/media/ || echo "clean: media"
# interaction/ 不许 import 上层
git grep -n "from '\.\./viewer/" -- src/interaction/ || echo "clean: interaction"
```

Expected: 三条 `clean:`

**`renderer/` 不在链条里** —— 它是 `core/` 的消费者，被 `viewer/` 使用，和 `media/` 平级。**它唯一有权 import 的是 `core/`。**

- [ ] **Step 3: 包体积**

```bash
npm run build
ls -la dist/
gzip -c dist/pano-gl.min.js 2>/dev/null | wc -c || gzip -c dist/pano-gl.js | wc -c
```

**记下这个数，写进 README。** 一个渲染库的体积是使用者选型时会看的东西，而且**它现在是个具体的数，不是「很小」**。

- [ ] **Step 4: 最后确认三个门禁的记录还在**

```bash
ls test/integration/gate-*.test.ts
git log --oneline --all --grep="gate"
```

Expected: 三条门禁的测试文件都在，且都能追溯到通过的提交。

**把它们在 README 或 `docs/` 里点名** —— 这三个测试是「1.0.0 与 0.2.x 渲染同样的像素」这句话的全部依据。**别人（和未来的你）需要知道去信任什么。**

- [ ] **Step 5: 打 tag**

```bash
git tag -a v1.0.0 -m "pano.gl 1.0.0"
npm publish --dry-run
```

**`--dry-run` 先看一遍包内容**：

```
npm notice === Tarball Contents ===
```

Expected: 只有 `dist/` 下的文件、`package.json`、`README.md`、`LICENSE`。**不该有 `demo/`、`legacy/`、`docs/`、`test/`。**

- [ ] **Step 6: 最终验证**

```bash
rm -rf node_modules dist
npm ci
npm run typecheck && npm run test:unit && npm run test:integration && npm run build
```

Expected: 全绿，退出码 0。

---

## 完成标准

对照 spec §10.2 的 P7 行逐条核：

- [ ] **依赖 = 3**：`npm ls --omit=dev --all` 恰好 `gl-matrix` + `debug` + `ms`
- [ ] **无死代码**：`tsc --noUnusedLocals` 与 `knip` 都干净
- [ ] **文档更新**：README 重写、迁移指南存在、README 的示例**实际跑过**
- [ ] **旧代码删除**：`legacy/`、`webpack/`、`vendor/`、`.babelrc`、`.travis.yml` 都不在
- [ ] **CI 在跑测试**，且 WebGPU 路径真的被走到（不是静默降级到 WebGL2）
- [ ] **覆盖率 ≥90% 分支**，且是脚本在卡，不是人在看
- [ ] **`npm publish --dry-run` 的包内容干净**

## 明确的非目标

- **不写新功能。** 这一期要写新代码，就是前面漏了。
- **不做性能优化。** 有基准数据（P0）在，要优化是 1.1 的事。
- **不删 `demo/image/` 和 `demo/video/`。** 它们是演示素材，而且 `.npmignore` 已经把 `demo` 排除在包外。
- **不为了「更干净」重命名现有模块。** 分层的边界已经由计划定死，改名只会让 `git log` 更难跟。
